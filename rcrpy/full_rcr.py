"""rcrpy — standalone single-file build.

Robust Chauvenet Rejection (RCR) — Python port of the C++ algorithm in
Maples et al. 2018 (https://arxiv.org/abs/1807.05276). This file is a
drop-in single-file artifact: vendor it into a larger project, no
`pip install rcrpy` required.

Generated by `rcrpy/scripts/build_single_file.py`. To refresh after
changes to the `rcrpy` package source, re-run that script — do NOT
hand-edit this file.

Runtime dependencies: numpy>=1.26, scipy>=1.11. Python>=3.11.

The public API mirrors the `rcrpy` package:

    RCR, RCRResults, RejectionTech, MuType
    FunctionalForm, FunctionalFormResults, Priors, PriorType
    NonParametric

See https://github.com/nickk124/RCR for the C++ reference implementation
and Maples et al. 2018 for the algorithm.
"""
from __future__ import annotations

# fmt: off  # generated file; don't reformat

# ---- imports ----
from dataclasses import dataclass, field
from enum import Enum
from math import comb
from scipy.optimize import least_squares, fsolve
from typing import Callable, Sequence
from typing import Sequence
import itertools
import math
import numpy as np

# ============================================================================
# Module: tables.py
# ============================================================================



ESUnity = np.array([
    0, 0, 0, 0, 0, 8.1666, 5.0808, 6.60453, 6.57429, 5.86414,
    4.75354, 4.31763, 3.95886, 3.67176, 3.61546, 3.27558, 3.1039, 2.9816, 2.7951, 2.71315,
    2.65111, 2.60358, 2.55717, 2.48317, 2.44219, 2.42666, 2.34112, 2.34151, 2.3212, 2.27048,
    2.2469, 2.21856, 2.18775, 2.17785, 2.18495, 2.14245, 2.13495, 2.12995, 2.12228, 2.06979,
    2.08234, 2.06751, 2.05053, 2.04679, 2.04511, 2.03733, 2.00788, 1.99989, 1.98854, 1.99807,
    1.97196, 1.96665, 1.96873, 1.96407, 1.97134, 1.97476, 1.9479, 1.93348, 1.93729, 1.94159,
    1.93376, 1.93016, 1.92467, 1.90928, 1.91914, 1.90782, 1.89641, 1.91532, 1.90886, 1.92046,
    1.89849, 1.90122, 1.89933, 1.89731, 1.89772, 1.88835, 1.87918, 1.87955, 1.88165, 1.88726,
    1.86683, 1.87656, 1.88028, 1.88149, 1.87248, 1.88399, 1.86493, 1.87471, 1.86693, 1.86201,
    1.86646, 1.86772, 1.86229, 1.85577, 1.87048, 1.86354, 1.84507, 1.8591, 1.85134, 1.8582,
    1.83742, 1.85959, 1.84882, 1.85987, 1.86173, 1.83974, 1.86365, 1.84821, 1.85502, 1.85445,
    1.85968, 1.85108, 1.85655, 1.85065, 1.85595, 1.85469, 1.87006, 1.87031, 1.87044, 1.83924,
    1.8517, 1.84286, 1.84867, 1.86211, 1.85371, 1.86309, 1.85043, 1.85959, 1.85356, 1.86067,
    1.86836, 1.86463, 1.86499, 1.86168, 1.85547, 1.85889, 1.85769, 1.86705, 1.86334, 1.84789,
    1.86541, 1.84999, 1.87105, 1.86007, 1.87412, 1.85801, 1.86571, 1.86972, 1.87379, 1.8744,
    1.8554, 1.86018, 1.8826, 1.84798, 1.88937, 1.89387, 1.86612, 1.87548, 1.88121, 1.88415,
    1.87015, 1.8861, 1.88231, 1.86763, 1.87352, 1.87648, 1.86567, 1.87065, 1.86588, 1.89753,
    1.87486, 1.87875, 1.85801, 1.8802, 1.87334, 1.88844, 1.89476, 1.90011, 1.8868, 1.90092,
    1.88328, 1.89275, 1.87378, 1.89406, 1.90745, 1.89517, 1.89659, 1.89442, 1.89757, 1.91347,
    1.873, 1.91664, 1.90175, 1.90809, 1.90104, 1.89359, 1.91446, 1.90136, 1.89328, 1.90306,
    1.91074, 1.92347, 1.88847, 1.90926, 1.90457, 1.90546, 1.92029, 1.90872, 1.91051, 1.89849,
    1.90966, 1.90013, 1.91262, 1.89846, 1.91245, 1.91623, 1.92408, 1.92469, 1.91827, 1.91272,
    1.92312, 1.90166, 1.93004, 1.92804, 1.91557, 1.92108, 1.9281, 1.91955, 1.91109, 1.90594,
    1.94078, 1.9506, 1.92283, 1.92585, 1.92885, 1.92967, 1.94285, 1.92227, 1.93435, 1.93533,
    1.92872, 1.93116, 1.95184, 1.95929, 1.93613, 1.93753, 1.95813, 1.94259, 1.94857, 1.93827,
    1.9403, 1.95039, 1.9599, 1.94687, 1.94243, 1.93983, 1.95263, 1.94929, 1.96734, 1.95456,
    1.96577, 1.94643, 1.95825, 1.95878, 1.95404, 1.95153, 1.94547, 1.96893, 1.9594, 1.97205,
    1.99737, 1.96382, 1.976, 1.98593, 1.9721, 1.97738, 1.98635, 1.96346, 1.97599, 1.97878,
    1.99636, 1.97553, 1.98378, 1.96246, 1.97884, 1.9695, 1.98307, 1.97067, 1.99561, 1.99559,
    1.97476, 1.98626, 1.9757, 1.98518, 1.9889, 1.98579, 1.9907, 1.98393, 1.98949, 1.97859,
    1.98883, 1.99809, 2.01698, 1.97457, 2.00691, 1.99878, 1.98738, 1.98681, 2.02094, 2.00732,
    2.01242, 2.00143, 2.01719, 2.00147, 2.01433, 1.98978, 2.01423, 2.02388, 1.99763, 2.01322,
    2.01779, 2.00585, 2.0262, 2.01612, 2.015, 2.01545, 2.02878, 1.9953, 2.02431, 2.00385,
    2.01857, 2.01121, 2.00012, 2.04458, 2.01686, 2.03361, 2.03484, 2.01569, 2.02847, 2.03367,
    2.01199, 2.03147, 2.02107, 2.02272, 2.02693, 2.02361, 2.05638, 2.05819, 2.03968, 2.06116,
    2.03919, 2.057, 2.04157, 2.03594, 2.04629, 2.04504, 2.05291, 2.0337, 2.03813, 2.05083,
    2.05647, 2.04183, 2.06155, 2.03937, 2.03977, 2.0622, 2.05104, 2.03419, 2.03653, 2.04335,
    2.05126, 2.05407, 2.04052, 2.0543, 2.06232, 2.06837, 2.05954, 2.07001, 2.06376, 2.05613,
    2.08693, 2.06129, 2.06122, 2.07765, 2.06185, 2.07341, 2.09579, 2.07121, 2.06034, 2.05612,
    2.07538, 2.09243, 2.07411, 2.0629, 2.07861, 2.08307, 2.07181, 2.06602, 2.068, 2.09641,
    2.07932, 2.08298, 2.08073, 2.08581, 2.0851, 2.10295, 2.08136, 2.07008, 2.09438, 2.09176,
    2.08998, 2.08718, 2.10746, 2.07256, 2.08746, 2.0944, 2.10528, 2.08079, 2.08737, 2.10175,
    2.09705, 2.08389, 2.10981, 2.11035, 2.11369, 2.08858, 2.10032, 2.10094, 2.10825, 2.11005,
    2.10437, 2.11447, 2.12606, 2.11678, 2.1413, 2.09585, 2.12634, 2.11713, 2.10869, 2.12638,
    2.12402, 2.12494, 2.10982, 2.11702, 2.13528, 2.12185, 2.1013, 2.12729, 2.11141, 2.11924,
    2.11256, 2.11446, 2.12816, 2.12969, 2.11749, 2.14418, 2.10116, 2.13384, 2.13099, 2.14597,
    2.13887, 2.13683, 2.14822, 2.15896, 2.13273, 2.13917, 2.1528, 2.13894, 2.14315, 2.12441,
    2.13479, 2.13607, 2.14126, 2.14049, 2.16284, 2.13623, 2.15132, 2.16334, 2.15033, 2.14009,
    2.14827, 2.14371, 2.14474, 2.147, 2.15871, 2.13863, 2.15408, 2.15092, 2.13819, 2.18943,
    2.16572, 2.15846, 2.14524, 2.17515, 2.15688, 2.1765, 2.1592, 2.1673, 2.15585, 2.17656,
    2.16368, 2.17599, 2.17565, 2.16999, 2.17862, 2.17052, 2.19717, 2.16832, 2.16011, 2.18991,
    2.17384, 2.16112, 2.17405, 2.1751, 2.19438, 2.16823, 2.16627, 2.16182, 2.18889, 2.158,
    2.18684, 2.19179, 2.20725, 2.1885, 2.18414, 2.16727, 2.19537, 2.19736, 2.16647, 2.19236,
    2.17266, 2.2053, 2.18514, 2.20062, 2.20196, 2.20121, 2.20077, 2.18712, 2.1855, 2.203,
    2.19592, 2.19697, 2.19238, 2.21358, 2.20131, 2.20271, 2.2132, 2.20322, 2.18998, 2.18898,
    2.2032, 2.21105, 2.20624, 2.21042, 2.2051, 2.21223, 2.25237, 2.21964, 2.20576, 2.19588,
    2.20689, 2.20957, 2.20479, 2.21863, 2.20926, 2.19959, 2.22527, 2.23337, 2.23426, 2.21943,
    2.21984, 2.23413, 2.20928, 2.21516, 2.21589, 2.21113, 2.23569, 2.23778, 2.22916, 2.23161,
    2.23586, 2.2355, 2.21636, 2.21727, 2.23536, 2.24593, 2.23659, 2.24643, 2.22351, 2.23788,
    2.22869, 2.23329, 2.23923, 2.23153, 2.22083, 2.23567, 2.24503, 2.23027, 2.25075, 2.2466,
    2.2261, 2.26533, 2.23839, 2.24523, 2.25686, 2.25003, 2.26011, 2.23715, 2.24906, 2.2572,
    2.24812, 2.26422, 2.25018, 2.23922, 2.26886, 2.249, 2.25936, 2.27496, 2.27758, 2.24577,
    2.254, 2.25192, 2.26225, 2.26882, 2.25137, 2.25483, 2.29103, 2.26272, 2.26391, 2.2784,
    2.2738, 2.26043, 2.25974, 2.25427, 2.26472, 2.26555, 2.27494, 2.28194, 2.28255, 2.26372,
    2.27794, 2.2836, 2.28825, 2.29874, 2.28286, 2.27046, 2.29208, 2.26638, 2.27392, 2.28982,
    2.27952, 2.29714, 2.27305, 2.28039, 2.29303, 2.29785, 2.28801, 2.274, 2.29605, 2.29291,
    2.28823, 2.27533, 2.30257, 2.2948, 2.29129, 2.29115, 2.29554, 2.30859, 2.29487, 2.29137,
    2.29391, 2.29261, 2.30659, 2.27934, 2.30991, 2.28966, 2.3074, 2.29334, 2.30214, 2.30023,
    2.29928, 2.30966, 2.29758, 2.30252, 2.3262, 2.31343, 2.31017, 2.30876, 2.30309, 2.30082,
    2.30767, 2.30642, 2.31165, 2.33401, 2.31506, 2.30913, 2.30838, 2.31412, 2.31942, 2.32585,
    2.31608, 2.34261, 2.32858, 2.33181, 2.31739, 2.32328, 2.32405, 2.34106, 2.33121, 2.35545,
    2.34668, 2.33791, 2.36319, 2.33368, 2.32436, 2.33373, 2.31906, 2.32447, 2.33932, 2.3463,
    2.31751, 2.32482, 2.34584, 2.31332, 2.33577, 2.32814, 2.33068, 2.33119, 2.33357, 2.32783,
    2.33489, 2.33765, 2.32657, 2.34187, 2.34125, 2.33287, 2.36289, 2.37368, 2.33286, 2.36672,
    2.32112, 2.35406, 2.37977, 2.3474, 2.34621, 2.35117, 2.33558, 2.36639, 2.343, 2.33953,
    2.36846, 2.36019, 2.36664, 2.3685, 2.34536, 2.38779, 2.34615, 2.35924, 2.35619, 2.35674,
    2.38124, 2.35764, 2.3527, 2.37836, 2.35709, 2.37681, 2.3599, 2.35993, 2.37643, 2.34256,
    2.37494, 2.36611, 2.3678, 2.38232, 2.37103, 2.38294, 2.36588, 2.36488, 2.39838, 2.37459,
    2.39982, 2.37237, 2.39043, 2.37584, 2.36484, 2.36998, 2.39262, 2.39616, 2.38953, 2.38486,
    2.37668, 2.40776, 2.39609, 2.38562, 2.39695, 2.38721, 2.36337, 2.4008, 2.37085, 2.40936,
    2.37824, 2.39494, 2.37432, 2.39886, 2.40034, 2.38783, 2.37574, 2.3829, 2.39571, 2.37724,
    2.38703, 2.38019, 2.38043, 2.41066, 2.37786, 2.41555, 2.38219, 2.3991, 2.39931, 2.41541,
    2.40905, 2.38504, 2.37377, 2.39863, 2.39556, 2.38955, 2.40792, 2.40224, 2.41852, 2.40423,
    2.42243, 2.3974, 2.42301, 2.41379, 2.40933, 2.43269, 2.40928, 2.42571, 2.40922, 2.38589,
    2.43387, 2.43566, 2.40253, 2.40858, 2.43298, 2.3988, 2.41652, 2.42128, 2.41883, 2.41381,
    2.42592, 2.43009, 2.41947, 2.42634, 2.43983, 2.41319, 2.4213, 2.43972, 2.40919, 2.43407,
    2.43571, 2.43616, 2.42489, 2.41693, 2.42638, 2.44588, 2.40138, 2.44067, 2.41251, 2.4253,
    2.44413, 2.45559, 2.4249, 2.44772, 2.42393, 2.42194, 2.42421, 2.43479, 2.44285, 2.43805,
    2.43972, 2.44475, 2.44152, 2.43006, 2.42564, 2.4388, 2.46016, 2.44581, 2.45389, 2.44211,
    2.44729, 2.42274, 2.46739, 2.4538, 2.43008, 2.43388, 2.44605, 2.4527, 2.4542, 2.45912,
    2.43721, 2.44794, 2.44753, 2.45321, 2.4611, 2.45027, 2.42977, 2.45646, 2.43959, 2.44658,
    2.46308, 2.4674, 2.44387, 2.47152, 2.479, 2.45405, 2.4617, 2.48285, 2.46095, 2.45023,
    2.45919, 2.4687, 2.47058, 2.45913, 2.48053, 2.47406, 2.45633, 2.47714, 2.48744, 2.46831,
    2.49996, 2.48066, 2.46329, 2.47937, 2.49151, 2.4485, 2.44884, 2.4903, 2.47449, 2.49261,
    2.52088, 2.47321, 2.50311, 2.47982, 2.48593, 2.48404, 2.48011, 2.46479, 2.46686, 2.47759,
    2.49516, 2.46586, 2.48583, 2.47507, 2.49261, 2.51366, 2.49466, 2.48851, 2.48084, 2.47907,
    2.49216, 2.49586, 2.48606, 2.48748, 2.50659, 2.50226, 2.49907, 2.47668, 2.49408, 2.4747,
    2.49685, 2.47902, 2.48556, 2.52355, 2.51863, 2.50303, 2.49677, 2.49893, 2.495, 2.48216,
    2.49511, 2.49817, 2.5022, 2.50799, 2.48675, 2.51814, 2.50713, 2.52177, 2.49259, 2.51174,
    2.50533, 2.50638, 2.5212, 2.52129, 2.51768, 2.51824, 2.49155, 2.50844, 2.50625, 2.48858,
    2.50689,
], dtype=np.float64)

SSUnity = np.array([
    0, 0, 0, 0, 2.15681, 8.81255, 2.72685, 3.45589, 2.32749, 2.90262,
    2.41888, 2.76084, 2.37034, 2.47951, 2.3207, 2.41864, 2.21943, 2.33924, 2.18108, 2.25062,
    2.14112, 2.1984, 2.09772, 2.13145, 2.08836, 2.12187, 2.03728, 2.08679, 2.05492, 2.05858,
    2.04114, 2.06094, 2.02109, 2.02455, 1.99134, 2.01884, 2.01627, 2.0241, 1.96923, 2.01425,
    1.9681, 1.98454, 1.97773, 1.99193, 1.95293, 1.96469, 1.97371, 1.99114, 1.93276, 1.96951,
    1.9455, 1.96897, 1.96819, 1.94352, 1.9391, 1.97577, 1.94596, 1.94278, 1.95162, 1.94947,
    1.9354, 1.95432, 1.94189, 1.93058, 1.92598, 1.93135, 1.92705, 1.94832, 1.94477, 1.93304,
    1.91891, 1.92816, 1.93071, 1.92636, 1.92686, 1.94204, 1.91158, 1.93024, 1.93606, 1.94116,
    1.94119, 1.92857, 1.92096, 1.94413, 1.90939, 1.90963, 1.91973, 1.92188, 1.92554, 1.92088,
    1.91027, 1.93532, 1.92246, 1.92766, 1.90794, 1.91497, 1.92607, 1.93512, 1.91289, 1.94698,
    1.92398, 1.93819, 1.91674, 1.93119, 1.93532, 1.91059, 1.91698, 1.91589, 1.92052, 1.91982,
    1.90968, 1.90836, 1.91082, 1.91337, 1.8941, 1.92452, 1.92157, 1.90225, 1.90111, 1.89789,
    1.90852, 1.91614, 1.90442, 1.90563, 1.89635, 1.91467, 1.89847, 1.91982, 1.90748, 1.93264,
    1.89417, 1.91169, 1.89229, 1.92045, 1.9015, 1.90308, 1.91328, 1.90585, 1.90799, 1.90027,
    1.90334, 1.90647, 1.90834, 1.90034, 1.91302, 1.91276, 1.9122, 1.91081, 1.88134, 1.902,
    1.91332, 1.90927, 1.92343, 1.91227, 1.91807, 1.90075, 1.90756, 1.91373, 1.90331, 1.90487,
    1.90561, 1.91607, 1.92025, 1.91531, 1.91193, 1.89599, 1.91862, 1.90261, 1.90792, 1.90866,
    1.89406, 1.91595, 1.89274, 1.89967, 1.90876, 1.89717, 1.90859, 1.91391, 1.90559, 1.90095,
    1.90033, 1.90289, 1.90295, 1.90713, 1.90025, 1.88309, 1.90272, 1.91291, 1.90952, 1.8967,
    1.9081, 1.90577, 1.91587, 1.88008, 1.89939, 1.90212, 1.93413, 1.89113, 1.92183, 1.88261,
    1.91105, 1.91124, 1.91656, 1.91666, 1.89339, 1.90816, 1.90318, 1.90783, 1.89488, 1.91292,
    1.91516, 1.9127, 1.89191, 1.88983, 1.90066, 1.90265, 1.90542, 1.9038, 1.8986, 1.89256,
    1.88351, 1.8852, 1.92514, 1.88772, 1.9043, 1.9022, 1.89706, 1.89796, 1.89817, 1.88593,
    1.9075, 1.88818, 1.89838, 1.89365, 1.89466, 1.90708, 1.90425, 1.91575, 1.89347, 1.89518,
    1.90846, 1.91872, 1.90166, 1.89268, 1.90013, 1.90088, 1.90508, 1.90721, 1.92669, 1.90787,
    1.87833, 1.90566, 1.88696, 1.8908, 1.8995, 1.89255, 1.88287, 1.89618, 1.89197, 1.89878,
    1.88857, 1.88001, 1.88883, 1.88522, 1.91297, 1.89276, 1.89648, 1.89694, 1.9099, 1.89939,
    1.90364, 1.91509, 1.88928, 1.90324, 1.89895, 1.90954, 1.89269, 1.88783, 1.89777, 1.90334,
    1.88776, 1.87254, 1.90645, 1.90498, 1.90964, 1.89432, 1.90426, 1.91295, 1.91371, 1.90281,
    1.88529, 1.89394, 1.91211, 1.89553, 1.88646, 1.89605, 1.89684, 1.89165, 1.89019, 1.90702,
    1.90584, 1.8982, 1.89138, 1.88391, 1.91162, 1.89593, 1.87554, 1.89157, 1.89109, 1.91159,
    1.88954, 1.89924, 1.90355, 1.88566, 1.91358, 1.89022, 1.9111, 1.90806, 1.90755, 1.90232,
    1.91273, 1.8973, 1.9001, 1.89809, 1.87097, 1.906, 1.90316, 1.89894, 1.88788, 1.89129,
    1.91002, 1.89125, 1.88322, 1.88575, 1.91677, 1.89967, 1.88543, 1.90811, 1.90059, 1.89444,
    1.89978, 1.89799, 1.90427, 1.90272, 1.9061, 1.90819, 1.90026, 1.89927, 1.90479, 1.88755,
    1.88131, 1.89867, 1.90644, 1.88228, 1.88071, 1.90087, 1.90146, 1.87144, 1.91352, 1.9141,
    1.89858, 1.89862, 1.90519, 1.90058, 1.89918, 1.90559, 1.89944, 1.90062, 1.89251, 1.90507,
    1.88644, 1.89033, 1.88505, 1.88696, 1.87862, 1.90548, 1.89442, 1.90166, 1.89409, 1.89856,
    1.88847, 1.90425, 1.90832, 1.88451, 1.9019, 1.90881, 1.91162, 1.90154, 1.90307, 1.91407,
    1.88398, 1.91621, 1.88462, 1.90465, 1.87257, 1.89214, 1.90098, 1.90786, 1.88021, 1.90179,
    1.90588, 1.88598, 1.88188, 1.9037, 1.894, 1.90562, 1.9023, 1.90257, 1.91506, 1.88724,
    1.89661, 1.89036, 1.91581, 1.89118, 1.89605, 1.88842, 1.8849, 1.89656, 1.89162, 1.88283,
    1.89988, 1.89194, 1.89482, 1.9133, 1.89462, 1.89691, 1.89646, 1.90788, 1.91017, 1.87967,
    1.90209, 1.90721, 1.91069, 1.89462, 1.88581, 1.88022, 1.88962, 1.89843, 1.89884, 1.88286,
    1.90381, 1.89093, 1.89283, 1.92003, 1.89169, 1.91138, 1.89636, 1.91967, 1.8953, 1.90448,
    1.90181, 1.90356, 1.89381, 1.90262, 1.87854, 1.89806, 1.92212, 1.90101, 1.88927, 1.88755,
    1.89137, 1.89204, 1.90466, 1.89143, 1.90659, 1.90207, 1.9059, 1.90186, 1.89992, 1.90816,
    1.89709, 1.90251, 1.8906, 1.8974, 1.90416, 1.89726, 1.88361, 1.89477, 1.87703, 1.89657,
    1.9032, 1.8782, 1.90761, 1.90883, 1.89922, 1.88931, 1.90294, 1.91728, 1.8975, 1.90712,
    1.90092, 1.90051, 1.90849, 1.88904, 1.90892, 1.88063, 1.88996, 1.90751, 1.86961, 1.90242,
    1.90487, 1.89017, 1.89402, 1.90003, 1.88839, 1.89566, 1.90426, 1.90072, 1.89577, 1.88399,
    1.89207, 1.90252, 1.88967, 1.90138, 1.91235, 1.90589, 1.89749, 1.91055, 1.88218, 1.90668,
    1.90062, 1.88645, 1.91537, 1.88989, 1.87472, 1.90244, 1.91055, 1.90391, 1.8828, 1.89432,
    1.90331, 1.88782, 1.88651, 1.91428, 1.87854, 1.88879, 1.90204, 1.88806, 1.9132, 1.87505,
    1.89064, 1.89937, 1.90317, 1.87959, 1.87987, 1.91334, 1.89087, 1.90313, 1.88701, 1.88566,
    1.91023, 1.89054, 1.8945, 1.89948, 1.89636, 1.88011, 1.8943, 1.89894, 1.89616, 1.89537,
    1.88978, 1.88551, 1.90539, 1.89958, 1.88902, 1.8978, 1.88383, 1.89353, 1.87327, 1.89261,
    1.89132, 1.91017, 1.8932, 1.90353, 1.89165, 1.90393, 1.89115, 1.89779, 1.90125, 1.89697,
    1.88323, 1.8885, 1.89575, 1.91027, 1.89907, 1.88814, 1.90582, 1.88155, 1.90385, 1.88722,
    1.89629, 1.89471, 1.90323, 1.91022, 1.87994, 1.88495, 1.90931, 1.89433, 1.89826, 1.85809,
    1.89566, 1.90934, 1.89164, 1.88081, 1.8992, 1.87815, 1.89481, 1.90496, 1.87239, 1.89234,
    1.89378, 1.89824, 1.87987, 1.88727, 1.88735, 1.8909, 1.88136, 1.88493, 1.89821, 1.89608,
    1.90322, 1.89722, 1.90471, 1.90275, 1.90514, 1.89977, 1.89659, 1.8967, 1.89102, 1.89847,
    1.87468, 1.89986, 1.89101, 1.90189, 1.89183, 1.88167, 1.88848, 1.8998, 1.90078, 1.88937,
    1.89129, 1.89438, 1.88236, 1.88634, 1.90459, 1.88899, 1.9121, 1.89335, 1.88067, 1.89845,
    1.90379, 1.89761, 1.88957, 1.89529, 1.89919, 1.89856, 1.87728, 1.90058, 1.8912, 1.89529,
    1.90655, 1.89635, 1.89044, 1.90135, 1.89168, 1.88689, 1.88565, 1.88905, 1.88723, 1.87604,
    1.91439, 1.90133, 1.90071, 1.90261, 1.89697, 1.89686, 1.90015, 1.90973, 1.89383, 1.89142,
    1.8829, 1.87547, 1.88567, 1.87654, 1.90044, 1.89668, 1.88688, 1.8951, 1.90837, 1.90282,
    1.8893, 1.88972, 1.89732, 1.89699, 1.89371, 1.91153, 1.90202, 1.89278, 1.88933, 1.88005,
    1.9068, 1.89558, 1.89649, 1.89931, 1.89903, 1.89768, 1.8823, 1.88875, 1.89121, 1.90444,
    1.90323, 1.87558, 1.90504, 1.90253, 1.90053, 1.89214, 1.90118, 1.88344, 1.89658, 1.89599,
    1.89615, 1.88639, 1.88376, 1.88869, 1.89978, 1.90987, 1.88091, 1.91121, 1.88613, 1.88337,
    1.89822, 1.88458, 1.91133, 1.88807, 1.89796, 1.89759, 1.90655, 1.88328, 1.89338, 1.90343,
    1.89968, 1.8861, 1.90912, 1.89127, 1.87304, 1.88516, 1.88347, 1.91319, 1.89017, 1.8873,
    1.90613, 1.89508, 1.87199, 1.90935, 1.90179, 1.89347, 1.88066, 1.88283, 1.91498, 1.9015,
    1.886, 1.90733, 1.89976, 1.88605, 1.89132, 1.8883, 1.90426, 1.88447, 1.89635, 1.88897,
    1.90566, 1.89833, 1.90698, 1.89831, 1.88439, 1.8969, 1.89293, 1.88194, 1.87484, 1.89074,
    1.90319, 1.91112, 1.88657, 1.9033, 1.89549, 1.88448, 1.90199, 1.89799, 1.87417, 1.88978,
    1.8832, 1.88876, 1.89214, 1.88684, 1.90323, 1.9013, 1.89762, 1.87905, 1.88232, 1.90718,
    1.87753, 1.9065, 1.90041, 1.88791, 1.9134, 1.8941, 1.89439, 1.90251, 1.89448, 1.90285,
    1.89068, 1.88837, 1.89769, 1.88539, 1.88133, 1.90799, 1.89069, 1.89388, 1.89132, 1.90184,
    1.87038, 1.90038, 1.90289, 1.89678, 1.88616, 1.90152, 1.89762, 1.89782, 1.9074, 1.89764,
    1.89548, 1.88731, 1.90852, 1.90634, 1.88099, 1.91536, 1.89966, 1.87809, 1.88994, 1.88381,
    1.89081, 1.88562, 1.89853, 1.90482, 1.88468, 1.90035, 1.88939, 1.89295, 1.90462, 1.89681,
    1.88103, 1.88996, 1.89812, 1.90114, 1.88874, 1.90127, 1.87943, 1.88883, 1.89715, 1.90068,
    1.8923, 1.88333, 1.91323, 1.89676, 1.88366, 1.89779, 1.91161, 1.88421, 1.89144, 1.90206,
    1.88643, 1.90517, 1.90919, 1.89141, 1.88577, 1.89182, 1.885, 1.90218, 1.89239, 1.90625,
    1.88104, 1.89626, 1.89945, 1.88308, 1.90484, 1.89541, 1.87879, 1.89445, 1.89553, 1.88854,
    1.88014, 1.91178, 1.91681, 1.90214, 1.89259, 1.89686, 1.88722, 1.89506, 1.88619, 1.89281,
    1.89551, 1.8855, 1.88574, 1.90563, 1.90001, 1.88354, 1.88374, 1.90892, 1.87765, 1.89739,
    1.90702, 1.89368, 1.89146, 1.89516, 1.8783, 1.89885, 1.89899, 1.88313, 1.90027, 1.90857,
    1.89404, 1.90807, 1.90308, 1.87233, 1.88823, 1.8987, 1.87895, 1.88939, 1.8966, 1.90515,
    1.8941, 1.8914, 1.88204, 1.902, 1.91323, 1.89711, 1.87751, 1.89697, 1.89793, 1.88654,
    1.88197, 1.89874, 1.89499, 1.88469, 1.88786, 1.88063, 1.89704, 1.89433, 1.89479, 1.89705,
    1.8853, 1.90603, 1.89423, 1.91276, 1.88896, 1.89314, 1.89921, 1.88988, 1.90855, 1.8792,
    1.89072, 1.88432, 1.87707, 1.91261, 1.90356, 1.88729, 1.8989, 1.88147, 1.88846, 1.90401,
    1.90417, 1.88635, 1.88752, 1.90154, 1.87506, 1.8958, 1.89085, 1.88669, 1.90684, 1.87381,
    1.88418, 1.8935, 1.89989, 1.87953, 1.89611, 1.88679, 1.89886, 1.8935, 1.89282, 1.9088,
    1.91984, 1.88528, 1.90889, 1.89211, 1.88465, 1.9064, 1.88655, 1.88969, 1.89699, 1.90052,
    1.87937,
], dtype=np.float64)

LSUnity = np.array([
    0, 0, 0, 0, 0, 36.8534, 35.9209, 34.563, 33.7065, 27.7158,
    19.4552, 15.9112, 12.9506, 11.4666, 10.9081, 9.57823, 8.10273, 7.26361, 6.34613, 5.77615,
    5.31425, 4.88903, 4.54831, 4.4066, 4.14193, 3.98687, 3.88145, 3.78224, 3.53612, 3.51816,
    3.41364, 3.31962, 3.23311, 3.23204, 3.1276, 3.09802, 3.0242, 3.01162, 2.91956, 2.92877,
    2.87747, 2.82572, 2.84595, 2.82653, 2.76119, 2.73985, 2.7262, 2.70399, 2.68546, 2.66641,
    2.66605, 2.62692, 2.59297, 2.58996, 2.58574, 2.56806, 2.52424, 2.54642, 2.51156, 2.52289,
    2.51645, 2.50626, 2.47096, 2.47922, 2.48032, 2.46965, 2.45449, 2.47265, 2.46335, 2.46095,
    2.46207, 2.44411, 2.44908, 2.44387, 2.42582, 2.43031, 2.41448, 2.40721, 2.4291, 2.3857,
    2.41122, 2.38928, 2.3845, 2.37153, 2.39368, 2.39749, 2.36566, 2.36452, 2.36156, 2.3567,
    2.37369, 2.36823, 2.38324, 2.36444, 2.35227, 2.35479, 2.36963, 2.34846, 2.36358, 2.36588,
    2.33237, 2.31503, 2.32805, 2.33006, 2.30914, 2.33301, 2.32591, 2.32715, 2.33602, 2.3315,
    2.32701, 2.31637, 2.31514, 2.30695, 2.32584, 2.33675, 2.33606, 2.32676, 2.3061, 2.31024,
    2.32392, 2.30527, 2.32664, 2.32271, 2.3182, 2.30015, 2.30045, 2.30848, 2.33988, 2.30429,
    2.30381, 2.30484, 2.30502, 2.30216, 2.29704, 2.32589, 2.30058, 2.32206, 2.32104, 2.31013,
    2.30736, 2.29894, 2.31059, 2.30877, 2.31979, 2.31853, 2.30831, 2.31743, 2.31835, 2.31956,
    2.29383, 2.3098, 2.32044, 2.31076, 2.32562, 2.31578, 2.31019, 2.31885, 2.31229, 2.31798,
    2.31418, 2.31711, 2.29978, 2.30664, 2.32066, 2.32196, 2.31512, 2.30798, 2.32331, 2.30176,
    2.29932, 2.30248, 2.30617, 2.31901, 2.32557, 2.31063, 2.31588, 2.3159, 2.31772, 2.31842,
    2.31526, 2.3276, 2.32912, 2.34188, 2.27696, 2.30697, 2.30451, 2.32676, 2.30071, 2.31046,
    2.31215, 2.32906, 2.32624, 2.31233, 2.31996, 2.31474, 2.33245, 2.32916, 2.32184, 2.32315,
    2.33396, 2.33788, 2.32957, 2.32283, 2.32555, 2.324, 2.31826, 2.31881, 2.32999, 2.31921,
    2.32627, 2.32733, 2.32873, 2.33075, 2.33536, 2.32293, 2.31342, 2.32611, 2.33335, 2.35003,
    2.33859, 2.32431, 2.32131, 2.36006, 2.32564, 2.34198, 2.31628, 2.33134, 2.35282, 2.3304,
    2.33832, 2.3339, 2.32174, 2.35165, 2.34869, 2.35611, 2.3312, 2.32226, 2.33333, 2.34529,
    2.33104, 2.33178, 2.34451, 2.35776, 2.33515, 2.36907, 2.36208, 2.3577, 2.35587, 2.37169,
    2.35331, 2.34057, 2.36204, 2.32979, 2.35296, 2.34675, 2.35479, 2.34909, 2.36341, 2.36852,
    2.35247, 2.35652, 2.34908, 2.34402, 2.34519, 2.35936, 2.34917, 2.35581, 2.37457, 2.36935,
    2.37624, 2.35, 2.36405, 2.37997, 2.36499, 2.36181, 2.37889, 2.37869, 2.37991, 2.37276,
    2.35524, 2.38919, 2.34321, 2.36569, 2.35995, 2.38254, 2.37014, 2.34551, 2.38429, 2.39698,
    2.37262, 2.36594, 2.36637, 2.38661, 2.38222, 2.35873, 2.37462, 2.38352, 2.37973, 2.39807,
    2.37303, 2.38523, 2.39735, 2.39825, 2.38887, 2.39099, 2.36962, 2.39097, 2.38772, 2.37725,
    2.39564, 2.38023, 2.3893, 2.39098, 2.39579, 2.36855, 2.37588, 2.41048, 2.39376, 2.38372,
    2.40223, 2.37983, 2.38342, 2.37161, 2.39749, 2.40345, 2.41587, 2.36511, 2.37592, 2.39139,
    2.41094, 2.39567, 2.40709, 2.41242, 2.38928, 2.41823, 2.39035, 2.40312, 2.41389, 2.40169,
    2.38431, 2.41435, 2.39744, 2.43255, 2.40659, 2.42163, 2.42772, 2.39879, 2.41731, 2.40878,
    2.40211, 2.43587, 2.38383, 2.40603, 2.43596, 2.40904, 2.42474, 2.4032, 2.41303, 2.41279,
    2.44204, 2.43031, 2.40928, 2.41103, 2.41396, 2.4162, 2.41199, 2.41941, 2.39934, 2.4284,
    2.43677, 2.41108, 2.42056, 2.43162, 2.42774, 2.42649, 2.4378, 2.43639, 2.44567, 2.42983,
    2.42878, 2.43716, 2.443, 2.42191, 2.43273, 2.42527, 2.44494, 2.43479, 2.4229, 2.44183,
    2.42446, 2.46586, 2.43181, 2.42846, 2.46485, 2.43435, 2.44537, 2.42507, 2.44751, 2.45449,
    2.43665, 2.46273, 2.43859, 2.46295, 2.47624, 2.42944, 2.46189, 2.44163, 2.41992, 2.43553,
    2.4875, 2.43687, 2.46288, 2.4557, 2.42297, 2.45249, 2.45016, 2.49956, 2.43977, 2.45717,
    2.46192, 2.47636, 2.46011, 2.46693, 2.46462, 2.46019, 2.4574, 2.45756, 2.45052, 2.44069,
    2.47748, 2.45291, 2.45148, 2.4435, 2.4392, 2.46629, 2.44054, 2.47332, 2.45765, 2.48058,
    2.47736, 2.47677, 2.47976, 2.45027, 2.47531, 2.44251, 2.49847, 2.4591, 2.45593, 2.49194,
    2.46848, 2.46689, 2.44947, 2.46825, 2.47907, 2.46224, 2.46655, 2.48657, 2.52373, 2.48287,
    2.46254, 2.49295, 2.48062, 2.46829, 2.48751, 2.49493, 2.47246, 2.47503, 2.49402, 2.47679,
    2.47079, 2.46039, 2.47582, 2.49556, 2.4796, 2.47735, 2.49906, 2.49884, 2.49199, 2.50527,
    2.4915, 2.48734, 2.49249, 2.4917, 2.46335, 2.48479, 2.4946, 2.49776, 2.48784, 2.46877,
    2.48966, 2.49595, 2.47949, 2.51807, 2.49209, 2.50142, 2.51039, 2.51679, 2.51214, 2.50556,
    2.50435, 2.48686, 2.48369, 2.49709, 2.49323, 2.50155, 2.50268, 2.50462, 2.50216, 2.50117,
    2.52367, 2.51911, 2.52693, 2.50703, 2.50557, 2.51375, 2.52358, 2.5186, 2.492, 2.53665,
    2.50627, 2.50134, 2.52079, 2.52392, 2.50891, 2.52027, 2.51075, 2.52028, 2.53086, 2.52228,
    2.50621, 2.52003, 2.55193, 2.52878, 2.50474, 2.50677, 2.52377, 2.52075, 2.49925, 2.52755,
    2.52391, 2.51727, 2.53087, 2.54322, 2.53557, 2.52718, 2.53461, 2.52847, 2.53709, 2.50712,
    2.53503, 2.52062, 2.53159, 2.53295, 2.50698, 2.5267, 2.51318, 2.53213, 2.51027, 2.54457,
    2.54982, 2.53728, 2.53525, 2.53594, 2.5311, 2.51435, 2.54877, 2.53915, 2.52995, 2.52399,
    2.54886, 2.55376, 2.56368, 2.5365, 2.52975, 2.55837, 2.56132, 2.54107, 2.53877, 2.53377,
    2.5597, 2.52129, 2.5522, 2.52495, 2.53839, 2.574, 2.52807, 2.55233, 2.55734, 2.55201,
    2.56279, 2.54147, 2.54816, 2.5406, 2.54299, 2.53876, 2.54489, 2.54608, 2.52993, 2.5548,
    2.54196, 2.55706, 2.56675, 2.55882, 2.54778, 2.55636, 2.56739, 2.55791, 2.54213, 2.57212,
    2.5587, 2.54781, 2.55629, 2.55217, 2.56069, 2.57395, 2.55695, 2.58945, 2.57694, 2.57801,
    2.56366, 2.55821, 2.55877, 2.56047, 2.58122, 2.54434, 2.57663, 2.56465, 2.56682, 2.5836,
    2.5914, 2.58477, 2.56708, 2.58162, 2.58168, 2.54164, 2.60683, 2.5715, 2.55917, 2.5675,
    2.58735, 2.57018, 2.58657, 2.60211, 2.55943, 2.58734, 2.57386, 2.54582, 2.55171, 2.59369,
    2.58087, 2.56467, 2.58683, 2.57931, 2.56709, 2.59857, 2.59497, 2.60257, 2.59976, 2.58045,
    2.60188, 2.57599, 2.58117, 2.57755, 2.5852, 2.55517, 2.58127, 2.56754, 2.59936, 2.54267,
    2.57909, 2.58412, 2.61833, 2.60542, 2.60639, 2.60918, 2.56979, 2.60049, 2.55136, 2.58865,
    2.58476, 2.56743, 2.59835, 2.58465, 2.6009, 2.60052, 2.60446, 2.61713, 2.60886, 2.58914,
    2.58108, 2.61292, 2.5944, 2.58891, 2.58533, 2.61508, 2.59526, 2.59377, 2.59972, 2.61878,
    2.60688, 2.6091, 2.59368, 2.61317, 2.59413, 2.60147, 2.60645, 2.61759, 2.60378, 2.6127,
    2.60983, 2.61437, 2.60514, 2.62028, 2.58728, 2.60484, 2.5839, 2.61168, 2.59679, 2.63831,
    2.60361, 2.59929, 2.62249, 2.6155, 2.62211, 2.63329, 2.62473, 2.62245, 2.5998, 2.61497,
    2.62168, 2.62665, 2.62334, 2.61483, 2.60848, 2.63601, 2.63043, 2.63654, 2.64424, 2.60307,
    2.63131, 2.61724, 2.63227, 2.64063, 2.64034, 2.64152, 2.61469, 2.62656, 2.6106, 2.60489,
    2.60702, 2.62082, 2.61918, 2.63803, 2.64378, 2.63804, 2.60679, 2.63425, 2.65165, 2.66037,
    2.61499, 2.64311, 2.64223, 2.62837, 2.63828, 2.63047, 2.65818, 2.63292, 2.65188, 2.64348,
    2.63508, 2.62616, 2.64721, 2.64219, 2.63744, 2.63433, 2.66162, 2.62189, 2.64546, 2.66013,
    2.63064, 2.64863, 2.64538, 2.63274, 2.61465, 2.63777, 2.65034, 2.63136, 2.66236, 2.63136,
    2.65056, 2.65583, 2.64835, 2.64503, 2.6594, 2.63609, 2.64269, 2.647, 2.65979, 2.65428,
    2.65347, 2.64373, 2.65751, 2.63515, 2.65027, 2.65868, 2.65317, 2.67415, 2.65978, 2.66258,
    2.66796, 2.68042, 2.66031, 2.63989, 2.66617, 2.65777, 2.65101, 2.67553, 2.66463, 2.65496,
    2.65991, 2.67406, 2.66285, 2.66322, 2.66159, 2.65272, 2.66115, 2.66574, 2.65239, 2.67235,
    2.6834, 2.68162, 2.66852, 2.64011, 2.65396, 2.67284, 2.65112, 2.6639, 2.66532, 2.66533,
    2.67687, 2.66272, 2.67128, 2.67501, 2.67628, 2.66219, 2.6629, 2.68378, 2.68541, 2.65607,
    2.68323, 2.65984, 2.69041, 2.67527, 2.6632, 2.68841, 2.67395, 2.65894, 2.65999, 2.66519,
    2.6833, 2.67351, 2.68194, 2.69945, 2.66126, 2.66342, 2.65907, 2.66376, 2.66585, 2.68072,
    2.69919, 2.69388, 2.67666, 2.6774, 2.67711, 2.67085, 2.65603, 2.67537, 2.66262, 2.7217,
    2.6979, 2.6986, 2.67352, 2.67068, 2.69057, 2.67911, 2.66769, 2.67372, 2.68415, 2.70737,
    2.67506, 2.67741, 2.68076, 2.68482, 2.6812, 2.6784, 2.67889, 2.69316, 2.69867, 2.72472,
    2.69523, 2.68489, 2.67129, 2.68269, 2.67858, 2.69434, 2.69306, 2.70113, 2.71862, 2.71305,
    2.68519, 2.70542, 2.69408, 2.68624, 2.68845, 2.7162, 2.71963, 2.70849, 2.7113, 2.70849,
    2.69117, 2.70271, 2.69867, 2.68649, 2.70945, 2.70773, 2.71815, 2.68871, 2.70891, 2.69272,
    2.69751, 2.69163, 2.70256, 2.69113, 2.72412, 2.71537, 2.706, 2.70608, 2.69244, 2.71601,
    2.69698, 2.69159, 2.69689, 2.70018, 2.72669, 2.70069, 2.69244, 2.7193, 2.70408, 2.74286,
    2.71983, 2.714, 2.70431, 2.73146, 2.71164, 2.71262, 2.73303, 2.70071, 2.76026, 2.74374,
    2.71454, 2.71946, 2.7078, 2.72343, 2.69729, 2.71403, 2.73635, 2.71698, 2.7057, 2.71981,
    2.70349, 2.71896, 2.7107, 2.73126, 2.72404, 2.73664, 2.72766, 2.71329, 2.72124, 2.70917,
    2.71533, 2.7305, 2.73146, 2.74711, 2.7491, 2.74117, 2.73801, 2.73024, 2.73127, 2.73464,
    2.70061, 2.73093, 2.7378, 2.74728, 2.73877, 2.72774, 2.73405, 2.72133, 2.72068, 2.73524,
    2.71521,
], dtype=np.float64)

ESDLUnityCF = np.array([
    1, 1, 0.799351, 0.944325, 0.854403, 0.785926, 0.736956, 0.722211, 0.720667, 0.725184,
    0.75123, 0.755033, 0.765896, 0.780944, 0.785441, 0.787935, 0.797322, 0.802557, 0.810293, 0.812691,
    0.817311, 0.820638, 0.828034, 0.828629, 0.830826, 0.838871, 0.841844, 0.839754, 0.848066, 0.848166,
    0.85345, 0.854075, 0.858788, 0.860696, 0.863621, 0.862731, 0.868193, 0.863195, 0.874256, 0.872882,
    0.877161, 0.873557, 0.879086, 0.880324, 0.882034, 0.881067, 0.88604, 0.883767, 0.888839, 0.887088,
    0.893795, 0.890033, 0.89339, 0.892609, 0.896503, 0.896514, 0.897336, 0.897637, 0.900154, 0.899507,
    0.90176, 0.903267, 0.908935, 0.901649, 0.906029, 0.905089, 0.914035, 0.906141, 0.90988, 0.909883,
    0.910747, 0.91246, 0.911705, 0.9112, 0.914824, 0.915461, 0.914989, 0.914235, 0.91751, 0.917076,
    0.917417, 0.918592, 0.920662, 0.92628, 0.917178, 0.920872, 0.923064, 0.922202, 0.927969, 0.924312,
    0.923029, 0.929101, 0.927667, 0.927667, 0.928384, 0.924162, 0.927004, 0.931712, 0.932939, 0.931967,
    0.928198,
], dtype=np.float64)

LSDLUnityCF = np.array([
    1, 1, 0.799351, 0.285521, 0.340779, 0.41601, 0.447745, 0.478807, 0.489568, 0.519816,
    0.530939, 0.558881, 0.545599, 0.555364, 0.548858, 0.568839, 0.573853, 0.589571, 0.59025, 0.612567,
    0.615455, 0.626054, 0.62771, 0.643118, 0.646556, 0.658871, 0.66007, 0.668774, 0.671144, 0.682491,
    0.679353, 0.692286, 0.693122, 0.699773, 0.699438, 0.712464, 0.711153, 0.713465, 0.717557, 0.726389,
    0.726362, 0.732999, 0.726632, 0.735104, 0.736027, 0.742498, 0.744048, 0.75067, 0.745452, 0.754027,
    0.75414, 0.761648, 0.756361, 0.768596, 0.762763, 0.767644, 0.771832, 0.770402, 0.77238, 0.780689,
    0.773244, 0.780089, 0.781802, 0.784195, 0.781847, 0.788101, 0.783922, 0.793554, 0.794663, 0.794824,
    0.797726, 0.796223, 0.797372, 0.802599, 0.799013, 0.803278, 0.802559, 0.807167, 0.808534, 0.808257,
    0.807184, 0.812206, 0.812232, 0.814208, 0.813478, 0.817792, 0.814158, 0.819564, 0.81924, 0.82205,
    0.818275, 0.82352, 0.823616, 0.825935, 0.823225, 0.828422, 0.825587, 0.831217, 0.826585, 0.83475,
    0.832441,
], dtype=np.float64)

LS68UnityCF = np.array([
    1, 1, 0.799351, 0.285521, 0.340779, 0.415995, 0.447563, 0.478511, 0.489028, 0.519352,
    0.530078, 0.558642, 0.566581, 0.591561, 0.592315, 0.614014, 0.621671, 0.644098, 0.640643, 0.658897,
    0.659947, 0.674892, 0.670956, 0.691888, 0.692636, 0.707093, 0.700091, 0.71671, 0.717005, 0.729161,
    0.720492, 0.734685, 0.739639, 0.744988, 0.742101, 0.755613, 0.754763, 0.761168, 0.76062, 0.768433,
    0.767037, 0.780292, 0.772455, 0.778726, 0.780526, 0.787753, 0.787528, 0.790481, 0.791204, 0.799592,
    0.790551, 0.80264, 0.805621, 0.806967, 0.803342, 0.811288, 0.810548, 0.816211, 0.810334, 0.820914,
    0.818009, 0.824183, 0.818457, 0.830039, 0.824181, 0.832788, 0.823924, 0.833908, 0.831748, 0.837215,
    0.83172, 0.841664, 0.837954, 0.84414, 0.836922, 0.845512, 0.84334, 0.849, 0.843154, 0.851267,
    0.849053, 0.853751, 0.846056, 0.855709, 0.852672, 0.859149, 0.852491, 0.85993, 0.857938, 0.863711,
    0.854912, 0.865029, 0.861066, 0.866604, 0.861614, 0.868421, 0.865454, 0.869022, 0.863025, 0.870568,
    0.868542,
], dtype=np.float64)

SSDLUnityCF = np.array([
    1, 1, 0.799351, 0.521918, 0.577339, 0.528513, 0.635396, 0.659658, 0.675483, 0.696538,
    0.73329, 0.727713, 0.757271, 0.768152, 0.775964, 0.783458, 0.80077, 0.797932, 0.810314, 0.81869,
    0.820111, 0.829854, 0.834026, 0.841326, 0.843973, 0.847922, 0.858153, 0.856903, 0.858529, 0.868727,
    0.862837, 0.871493, 0.876271, 0.875507, 0.880854, 0.878536, 0.885307, 0.887318, 0.890606, 0.890264,
    0.893034, 0.898365, 0.896602, 0.899384, 0.901048, 0.904264, 0.902966, 0.906581, 0.908435, 0.907805,
    0.91133, 0.913216, 0.912063, 0.913785, 0.916749, 0.915789, 0.918895, 0.919865, 0.920497, 0.921825,
    0.92345, 0.92432, 0.923952, 0.925951, 0.926259, 0.927963, 0.928288, 0.930179, 0.930933, 0.925191,
    0.934905, 0.932677, 0.933801, 0.934811, 0.934255, 0.936249, 0.935739, 0.931335, 0.93934, 0.939084,
    0.939209, 0.939624, 0.94091, 0.940528, 0.940834, 0.942504, 0.942799, 0.943895, 0.943777, 0.945166,
    0.944398, 0.945379, 0.946653, 0.94639, 0.946253, 0.947046, 0.948222, 0.944239, 0.949949, 0.948752,
    0.950152,
], dtype=np.float64)

SSConstants = np.array([
    [0.0, 0.0, 0.0, 0.0, .202399, -.29158, -.03321, -.18181],
    [0.0, 0.0, 0.0, 0.0, .464231, .26031, .363776, .454703],
], dtype=np.float64)

# ============================================================================
# Module: stats.py
# ============================================================================





# ---------------------------------------------------------------------------
# Numerical primitives
# ---------------------------------------------------------------------------

def _cpp_seqsum(arr: np.ndarray) -> float:
    """Sequential left-to-right summation matching C++'s naive `+=` loop.

    `np.sum` uses pairwise summation (numerically more accurate but a
    different bit pattern than C++). `np.cumsum(...)[-1]` is implemented
    as a sequential C loop, so it produces bit-identical results to
    C++'s ``double s = 0; for (i) s += arr[i];`` pattern. Required for
    bit-identical parity through the double-line sigma estimator
    (fitDL_w/mFinder_w) where summation drift cascades into ES_MODE_DL
    + parametric rejection decisions.
    """
    if arr.size == 0:
        return 0.0
    return float(np.cumsum(arr.astype(np.float64, copy=False))[-1])


def erfcCustom(x: float) -> float:
    """Closed-form Chebyshev-like approximation of erfc used by RCR. Ported
    from cpp/src/RCR.cpp:196. Do NOT replace with scipy.special.erfc — the
    rejection boundary depends on this exact approximation.

    Guards against Python's overflow-to-exception on enormous `x` (which
    can occur when parametric residuals on one side of mu collapse to a
    near-zero sigma and the rejection ratio explodes). C++ silently goes
    to 1/inf == 0; Python raises. We catch and return 0.0, matching the
    mathematical limit erfc(x) → 0 as x → ∞.
    """
    x = x / math.sqrt(2.0)
    inner = (
        1.0
        + x * (0.0705230784
        + x * (0.0422820123
        + x * (0.0092705272
        + x * (0.0001520143
        + x * (0.0002765672
        + 0.0000430638 * x)))))
    )
    try:
        return 1.0 / inner ** 16
    except OverflowError:
        return 0.0


def isEqual(x: float, y: float,
            max_relative_error: float = 1e-8,
            max_absolute_error: float | None = None) -> bool:
    """Port of cpp/src/RCR.cpp:206. Default abs tolerance is DBL_MIN, which is
    effectively zero — only the relative error path matters."""
    if max_absolute_error is None:
        max_absolute_error = np.finfo(float).tiny  # ~DBL_MIN
    if abs(x - y) < max_absolute_error:
        return True
    if abs(y) > abs(x):
        rel = abs((x - y) / y)
    else:
        rel = abs((x - y) / x) if x != 0 else float("inf")
    return rel <= max_relative_error


def isEqual_vec_scalar(a: np.ndarray, b: float,
                       max_relative_error: float = 1e-8) -> np.ndarray:
    """Vectorized `isEqual(a[i], b)` for an array `a` and scalar `b`.
    Same semantics as the scalar `isEqual`: an entry is "equal to b" iff
        |a[i] - b| < DBL_MIN  OR
        |a[i] - b| / max(|a[i]|, |b|)  <=  rel_tol.
    Returns a bool ndarray the same shape as a.
    """
    a = np.asarray(a, dtype=np.float64)
    tiny = np.finfo(np.float64).tiny
    diff = np.abs(a - b)
    abs_a = np.abs(a)
    abs_b = abs(b)
    larger = np.maximum(abs_a, abs_b)
    abs_close = diff < tiny
    # rel_diff is meaningful only when larger > 0; in the both-zero case
    # abs_close already returns True. Use np.where to dodge 0/0 warnings.
    safe = np.where(larger > 0, larger, 1.0)
    rel = diff / safe
    return abs_close | (rel <= max_relative_error)


def getDiff(mu: float, datum: float) -> float:
    return abs(datum - mu)


# ---------------------------------------------------------------------------
# Mu calculations (mean / median / half-sample-mode)
# ---------------------------------------------------------------------------

def getMean(y: np.ndarray) -> float:
    return float(np.sum(y) / y.size)


def getMean_w(w: np.ndarray, y: np.ndarray) -> float:
    return float(np.sum(w * y) / np.sum(w))


def getMedian(y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:505 — getMedian(std::vector<double> &y).

    The C++ assumes y is already sorted ascending. We preserve the exact
    linear-interpolation formula it uses (NOT numpy.median, even though for
    the standard cases they agree, because we want byte-identical output
    on parity tests).
    """
    n = y.size
    if n <= 1:
        return float(y[0])
    high = n // 2
    low = high - 1
    total_sum = n
    if n % 2 == 0:
        running_sum = n / 2.0 + 0.5
    else:
        running_sum = n / 2.0
    return float(y[low] + (0.5 * total_sum - running_sum + 1.0) * (y[high] - y[low]))


def getMedian_w(w: np.ndarray, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:474. Weighted median; assumes y is sorted
    ascending (and w is the weight at each y in that order)."""
    n = y.size
    if n <= 1:
        return float(y[0])
    total_sum = float(np.sum(w))
    sum_counter = 0
    running_sum = w[sum_counter] * 0.5
    while running_sum < 0.5 * total_sum:
        sum_counter += 1
        running_sum += w[sum_counter - 1] * 0.5 + w[sum_counter] * 0.5
    if sum_counter == 0:
        return float(y[0])
    denom = w[sum_counter - 1] * 0.5 + w[sum_counter] * 0.5
    return float(
        y[sum_counter - 1]
        + (0.5 * total_sum - (running_sum - denom)) / denom * (y[sum_counter] - y[sum_counter - 1])
    )


def _binarySearch(search_up: bool, minimum_index: int, to_find: float, to_search: np.ndarray) -> int:
    """Port of cpp/src/RCR.cpp:362. Custom binary search used by halfSampleMode."""
    if search_up:
        low, high = minimum_index, to_search.size
    else:
        low, high = 0, minimum_index
    low_in, high_in = -1, -1
    mid = 0
    while low != low_in or high != high_in:
        low_in, high_in = low, high
        mid = int(low + (high - low) / 2.0)
        if isEqual(to_find, float(to_search[mid])):
            low = mid
            high = mid
        elif to_find > to_search[mid]:
            low = mid
        elif to_find < to_search[mid]:
            high = mid
    return low if search_up else high


def halfSampleMode_w(w: np.ndarray, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:531 — getMode(trueCount, w, y), weighted.

    Vectorized. The C++ inner loop builds the cumulative half-weight vector
        s_vec[i] = w[0] + w[1] + ... + w[i-1] + 0.5 * w[i]
    (with the lower-bound offset), then for each i searches for the matching
    forward (branch 1) or backward (branch 2) k whose s_vec[k] differs from
    s_vec[i] by `half_weight_sum`. Branches 1 and 2 together iterate the same
    set of (i, k) endpoint pairs from both ends; vectorizing the forward
    sweep alone gives the same min-distance answer.

    Replacing the linear search with `np.searchsorted(side='right')` is
    correct because s_vec is monotonically non-decreasing (positive weights).
    """
    n = y.size
    lower_limit, upper_limit = 0, n - 1
    lower_limit_in, upper_limit_in = -1, -1
    REL_TOL = 1e-8

    while lower_limit != lower_limit_in or upper_limit != upper_limit_in:
        lower_limit_in = lower_limit
        upper_limit_in = upper_limit
        size = upper_limit - lower_limit + 1
        if size <= 1:
            break

        y_win = y[lower_limit:upper_limit + 1]
        w_win = w[lower_limit:upper_limit + 1]

        cumw = np.cumsum(w_win)
        s_vec = cumw - 0.5 * w_win
        half_weight_sum = float(cumw[-1]) * 0.5

        # Branch 1 forward sweep: for each i, find largest k with
        # s_vec[k] <= s_vec[i] + half_weight_sum. searchsorted with
        # side='right' returns the *first* index where the value would be
        # inserted to keep sorted order; subtract 1 to get the LAST index
        # that's <= the target. This matches the C++ linear forward search
        # (where the loop advances while still <= total, then steps back).
        totals = s_vec + half_weight_sum
        ks = np.searchsorted(s_vec, totals, side="right") - 1

        # Valid i values (branch 1 fires): s_vec[i] <= half_weight_sum
        # (with isEqual relative tolerance). For i past that, s_vec[i] + hws
        # exceeds the maximum s_vec value, ks[i] saturates at size-1, and
        # those candidates are dominated by branch-2-equivalent windows that
        # branch-1 already considered with smaller i. Mask them out.
        valid_b1 = (s_vec < half_weight_sum) | (
            np.abs(s_vec - half_weight_sum) <= REL_TOL * abs(half_weight_sum)
        )
        # Also drop i where the forward window doesn't reach the requested
        # weight (totals > s_vec[-1] beyond isEqual tolerance — symmetrically
        # those are covered by branch 2 from larger i).
        within_range = totals <= s_vec[-1] * (1.0 + REL_TOL) + REL_TOL
        valid = valid_b1 & within_range

        distances = np.where(valid, np.abs(y_win[ks] - y_win), np.inf)
        min_dist = float(np.min(distances))
        if not np.isfinite(min_dist):
            break  # degenerate; shouldn't occur for positive weights

        if min_dist == 0.0:
            tied = (distances == 0.0)
        else:
            tied = np.abs(distances - min_dist) <= REL_TOL * abs(min_dist)
        tied_idx = np.where(tied)[0]

        final_lower = int(tied_idx[0]) + lower_limit
        # Across ties, take the maximum upper endpoint (mirrors the
        # max(finalUpper, ...) expansion in the C++).
        final_upper = int(np.max(ks[tied_idx])) + lower_limit

        lower_limit, upper_limit = final_lower, final_upper

    window_y = y[lower_limit:upper_limit + 1]
    window_w = w[lower_limit:upper_limit + 1]
    return getMedian_w(window_w, window_y)


def inverf(x: float) -> float:
    """Port of cpp/src/RCR.cpp:187. RCR's custom inverse-erf approximation.
    Do NOT replace with scipy.special.erfinv — see notes on erfcCustom."""
    PI = 3.1415926535897932384626434
    inverf_mult = (8.0 * (PI - 3.0)) / (3 * PI * (4.0 - PI))
    sqrt2 = math.sqrt(2.0)
    x_log = math.log(1 - x * x)
    inner = (2.0 / (PI * inverf_mult) + x_log * 0.5) ** 2 - x_log / inverf_mult
    return sqrt2 * math.sqrt(-2.0 / (PI * inverf_mult) - x_log * 0.5 + math.sqrt(inner))


def countAmountLessThanOne(x: np.ndarray) -> int:
    """Port of cpp/src/RCR.cpp:342. Counts leading entries of x that are < 1.0,
    assuming x is monotonically nondecreasing (true for getXVec output)."""
    n = x.size
    if n == 1:
        return 0 if x[0] >= 1.0 else 1
    count = 0
    while count < n and x[count] < 1.0:
        count += 1
    return count


def getXVec_w(size: int, w: np.ndarray) -> np.ndarray:
    """Port of cpp/src/RCR.cpp:414. Weighted x-vector for single/double-line
    fits — inverse-erf of cumulative weight fractions."""
    w_sum = float(np.sum(w))
    s_sum = 0.682689 * float(w[0])
    inv_w_sum = 1.0 / w_sum
    x = np.empty(size, dtype=np.float64)
    x[0] = inverf(s_sum * inv_w_sum)
    for i in range(1, size):
        s_sum += 0.317311 * float(w[i - 1]) + 0.682689 * float(w[i])
        x[i] = inverf(s_sum * inv_w_sum)
    return x


def getOriginFixedRegressionLine_w(start: int, end: int, w: np.ndarray,
                                   x: np.ndarray, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:709. Slope of best-fit line through the
    origin: sum(w*x*y) / sum(w*x*x) over [start, end). Uses sequential
    summation matching C++'s `+=` accumulation order — required so the
    `single_line_fit` value (which becomes the sigma fallback in fitDL_w)
    is bit-identical to the C++."""
    w_s = w[start:end]
    x_s = x[start:end]
    y_s = y[start:end]
    wx = w_s * x_s
    num = _cpp_seqsum(wx * y_s)
    den = _cpp_seqsum(wx * x_s)
    return float(num / den)


def fitSL_w(w: np.ndarray, x: np.ndarray, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:968 — single-line fit, weighted."""
    return getOriginFixedRegressionLine_w(0, countAmountLessThanOne(x), w, x, y)


def mFinder_w(low: int, high: int, last_x_under_one: int, increment: int,
              w: np.ndarray, x: np.ndarray, y: np.ndarray) -> int:
    """Port of cpp/src/RCR.cpp:730. Each-m accumulators use sequential
    summation matching the C++'s naive `+=` order; required for
    bit-identical parity in the double-line sigma estimator (see
    fitDL_w's docstring for the full chain). The outer m-loop stays
    Python because each iteration depends on the running best-m via
    m_low/m_high.
    """
    DBL_MAX = float("inf")
    stop = False
    best_m = -1
    m_low = -1
    m_high = -1
    min_error = DBL_MAX
    while not stop:
        m = low
        while m < high:
            x_at_m = float(x[m])

            # Per-index term arrays in C++ left-then-right order. `a` and
            # `e` accumulate across both loops (cpp/src/RCR.cpp:747-762).
            w_l = w[:m + 1]
            x_l = x[:m + 1]
            y_l = y[:m + 1]
            wx_l = w_l * x_l
            a_terms_left = wx_l * x_l
            e_terms_left = wx_l * y_l

            end_r = last_x_under_one + 1
            w_r = w[m + 1:end_r]
            x_r = x[m + 1:end_r]
            y_r = y[m + 1:end_r]
            diff_r = x_r - x_at_m
            xatm_w_r = x_at_m * w_r
            a_terms_right = (x_at_m * x_at_m) * w_r
            b_terms = xatm_w_r * diff_r
            d_terms = (w_r * diff_r) * diff_r
            e_terms_right = xatm_w_r * y_r
            f_terms = (w_r * y_r) * diff_r

            a = _cpp_seqsum(np.concatenate([a_terms_left, a_terms_right]))
            e = _cpp_seqsum(np.concatenate([e_terms_left, e_terms_right]))
            b = _cpp_seqsum(b_terms)
            d = _cpp_seqsum(d_terms)
            f = _cpp_seqsum(f_terms)
            c = b
            if a != 0 and (d - c * b) != 0:
                tau = (f - e * c / a) / (d - c * b / a)
                sigma = (e - tau * b) / a

                factors_l = sigma * x_l - y_l
                err_terms_left = (w_l * factors_l) * factors_l
                factors_r = sigma * x_at_m + tau * diff_r - y_r
                err_terms_right = (w_r * factors_r) * factors_r
                error = _cpp_seqsum(
                    np.concatenate([err_terms_left, err_terms_right]))

                if error < min_error:
                    min_error = error
                    best_m = m
                    m_low = max(best_m - increment - 1, 1)
                    m_high = min(best_m + increment + 1, last_x_under_one)
            m += increment
        if increment > 1:
            increment = max(int(math.floor((m_high - m_low) / 6.36)), 1)
        else:
            stop = True
        low = m_low
        high = m_high
        min_error = DBL_MAX
    if low == high:
        return low
    return best_m


def fitDL_w(counter: float, w: np.ndarray, x: np.ndarray, y: np.ndarray,
            get_fn_w) -> float:
    """Port of cpp/src/RCR.cpp:5785. Weighted double-line sigma estimator.

    Uses sequential (left-to-right) summation via _cpp_seqsum to match
    the C++'s naive `+=` accumulation order bit-for-bit. The previous
    implementation used np.sum (pairwise) which drifted ~1e-15 per call
    and cascaded into 8-20% parameter divergence on ES_MODE_DL +
    parametric (where two independent sigma_below/sigma_above tracks
    amplify the drift). `a` and `e` accumulate across BOTH the left and
    right loops in C++; we concatenate the term arrays so a single
    cumsum produces the same order. Same for double_line_error.
    """
    amount_x_under_one = countAmountLessThanOne(x)
    single_line_fit = getOriginFixedRegressionLine_w(0, amount_x_under_one, w, x, y)
    if x.size < 4:
        return single_line_fit
    m = mFinder_w(1, amount_x_under_one - 1, amount_x_under_one - 1,
                  max(int(y.size / 6.36), 1), w, x, y)
    x_at_m = float(x[m])

    # Per-index term arrays. Multiplication order must match the C++
    # exactly: `wxProd = w[i]*x[i]; a += wxProd * x[i]` → `(w*x) * x`.
    # Left piece (i in [0, m+1)):
    w_l = w[:m + 1]
    x_l = x[:m + 1]
    y_l = y[:m + 1]
    wx_l = w_l * x_l
    a_terms_left = wx_l * x_l
    e_terms_left = wx_l * y_l

    # Right piece (i in [m+1, amount_x_under_one)):
    w_r = w[m + 1:amount_x_under_one]
    x_r = x[m + 1:amount_x_under_one]
    y_r = y[m + 1:amount_x_under_one]
    diff_r = x_r - x_at_m
    xatm_w_r = x_at_m * w_r
    a_terms_right = (x_at_m * x_at_m) * w_r
    b_terms = xatm_w_r * diff_r
    d_terms = (w_r * diff_r) * diff_r
    e_terms_right = xatm_w_r * y_r
    f_terms = (w_r * y_r) * diff_r

    # Sequential cumsum matching C++ `+=` order. `a` and `e` accumulate
    # across BOTH loops in C++ (lines 5797-5811), so concatenate before
    # summing.
    a = _cpp_seqsum(np.concatenate([a_terms_left, a_terms_right]))
    e = _cpp_seqsum(np.concatenate([e_terms_left, e_terms_right]))
    b = _cpp_seqsum(b_terms)
    d = _cpp_seqsum(d_terms)
    f = _cpp_seqsum(f_terms)
    c = b

    tau = (f - e * c / a) / (d - c * b / a)
    sigma = (e - tau * b) / a

    # Double-line error also accumulates across both loops in C++.
    factors_l = sigma * x_l - y_l
    err_terms_left = (w_l * factors_l) * factors_l
    factors_r = sigma * x_at_m + tau * diff_r - y_r
    err_terms_right = (w_r * factors_r) * factors_r
    double_line_error = _cpp_seqsum(
        np.concatenate([err_terms_left, err_terms_right]))

    # Single-line error: single loop in C++ (5828-5832).
    w_full = w[:amount_x_under_one]
    x_full = x[:amount_x_under_one]
    y_full = y[:amount_x_under_one]
    err_full = single_line_fit * x_full - y_full
    single_line_error = _cpp_seqsum(w_full * err_full * err_full)

    delta_chi_squared = (single_line_error - double_line_error) / double_line_error

    if sigma < 0:
        sigma = 1e-10
    if delta_chi_squared < get_fn_w(int(counter), x, w):
        sigma = single_line_fit
    return sigma


def getFNRatio(x: np.ndarray, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:167. Coefficient of variation of weights
    among entries with x[i] < 1."""
    counter = 0
    n = x.size
    while counter < n and x[counter] < 1.0:
        counter += 1
    mean = float(np.sum(w[:counter])) / counter
    st_dev = 0.0
    for i in range(counter):
        st_dev += (float(w[i]) - mean) ** 2
    st_dev = math.sqrt(st_dev / (counter - 1))
    return st_dev / mean


def getCFRatio(w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:151. Coefficient of variation of weights
    (sample stdev / mean), used inside weighted CFs."""
    n = w.size
    mean = float(np.sum(w)) / n
    diffs = w - mean
    st_dev = math.sqrt(float(np.sum(diffs * diffs)) / (n - 1))
    return st_dev / mean


def halfSampleMode(y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:622 — getMode(trueCount, y), unweighted.

    Vectorized rewrite. The C++ inner loop over `i` computes, for each
    candidate window start, the distance `y[i + half_window] - y[i]` and
    tracks the minimum (expanding the chosen range across `isEqual` ties).
    All of that compresses to one numpy subtraction + argmin/where with
    a relative-tolerance tie mask.

    The unweighted s_vec is the arithmetic sequence [0.5, 1.5, ..., size-0.5],
    which makes the binary-search-and-distance computation collapse to
    `windows[i] = y[lower + i + half_window] - y[lower + i]` for
    `i in [0, size - half_window)`. Both C++ branches (forward and backward)
    iterate the same set of windows, so vectorizing the forward sweep alone
    is sufficient.
    """
    n = y.size
    lower_limit, upper_limit = 0, n - 1
    lower_limit_in, upper_limit_in = -1, -1
    REL_TOL = 1e-8

    while lower_limit != lower_limit_in or upper_limit != upper_limit_in:
        lower_limit_in = lower_limit
        upper_limit_in = upper_limit
        size = upper_limit - lower_limit + 1
        half_window = size // 2

        # Edge case: tiny windows. half_window == 0 happens only for size==1,
        # which the outer convergence check would have already exited on.
        if half_window == 0:
            break

        # windows[i] = y[lower + i + half_window] - y[lower + i]
        # length = size - half_window
        # For an even-`size` window, this is the standard HSM construction.
        # For odd `size`, the C++ midpoint-i has BOTH branches fire with the
        # same distance — equivalent to "midpoint gets counted twice", which
        # doesn't change the argmin result.
        y_win = y[lower_limit:upper_limit + 1]
        windows = y_win[half_window:] - y_win[:size - half_window]

        min_dist = float(np.min(windows))

        # Tie expansion: the C++ uses isEqual (relative tol 1e-8) and tracks
        # min(finalLower) / max(finalUpper) over tied windows.
        if min_dist == 0.0:
            tied = (windows == 0.0)
        else:
            tied = np.abs(windows - min_dist) <= REL_TOL * abs(min_dist)
        tied_idx = np.where(tied)[0]
        final_lower = int(tied_idx[0]) + lower_limit
        final_upper = int(tied_idx[-1]) + half_window + lower_limit

        lower_limit = final_lower
        upper_limit = final_upper

    window = y[lower_limit:upper_limit + 1]
    return getMedian(window)


# ---------------------------------------------------------------------------
# Sigma calculations
# ---------------------------------------------------------------------------

def getStDev(delta: float, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:891. RMS-style estimator: sqrt(sum(y^2) / (n - delta))."""
    n = y.size
    top = float(np.sum(y * y))
    return math.sqrt(top / (n - delta))


def getStDev_w(delta: float, w: np.ndarray, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:878."""
    top = float(np.sum(w * y * y))
    w_sum = float(np.sum(w))
    w_sum_sq = float(np.sum(w * w))
    return math.sqrt(top / (w_sum - delta * w_sum_sq / w_sum))


def get68th(y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:934 — unweighted, sorts internally."""
    y_sorted = np.sort(y)
    n = y_sorted.size
    if n <= 1:
        return float(y_sorted[0])
    total_sum = float(n)
    sum_counter = 0
    running_sum = 1.0 * 0.682689
    while running_sum < 0.682689 * total_sum:
        sum_counter += 1
        running_sum += 1.0 * 0.317311 + 1.0 * 0.682689
    if sum_counter == 0:
        return float(y_sorted[0])
    denom = 1.0 * 0.317311 + 1.0 * 0.682689
    return float(
        y_sorted[sum_counter - 1]
        + (0.682689 * total_sum - (running_sum - denom)) / denom
        * (y_sorted[sum_counter] - y_sorted[sum_counter - 1])
    )


def get68th_w(w: np.ndarray, y: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:901 — weighted; sorts y ascending, taking w along."""
    idx = np.argsort(y, kind="stable")
    y_sorted = y[idx]
    w_sorted = w[idx]
    n = y_sorted.size
    if n <= 1:
        return float(y_sorted[0])
    total_sum = float(np.sum(w_sorted))
    sum_counter = 0
    running_sum = float(w_sorted[sum_counter] * 0.682689)
    while running_sum < 0.682689 * total_sum:
        sum_counter += 1
        running_sum += float(w_sorted[sum_counter - 1] * 0.317311 + w_sorted[sum_counter] * 0.682689)
    if sum_counter == 0:
        return float(y_sorted[0])
    denom = float(w_sorted[sum_counter - 1] * 0.317311 + w_sorted[sum_counter] * 0.682689)
    return float(
        y_sorted[sum_counter - 1]
        + (0.682689 * total_sum - (running_sum - denom)) / denom
        * (y_sorted[sum_counter] - y_sorted[sum_counter - 1])
    )


# ---------------------------------------------------------------------------
# Auxiliary checks
# ---------------------------------------------------------------------------

def distinctValuesCheck(flags: np.ndarray, y: np.ndarray) -> bool:
    """Port of cpp/src/RCR.cpp:219. True if there are at least 3 distinct
    values among the True-flagged entries of y. Uses isEqual for the
    distinctness test (NOT exact float equality)."""
    n = y.size
    i = 0
    while i < n and not flags[i]:
        i += 1
    if i >= n:
        return False
    a = float(y[i])
    b = a
    i += 1
    while i < n:
        if flags[i] and not isEqual(float(y[i]), a):
            b = float(y[i])
            break
        i += 1
    i += 1
    while i < n:
        if flags[i] and not isEqual(float(y[i]), a) and not isEqual(float(y[i]), b):
            return True
        i += 1
    return False

# ============================================================================
# Module: nonparametric.py
# ============================================================================




class NonParametric:
    """Base class for user-supplied non-parametric mu computation.

    Subclass and override `mu_func` (and/or `mu_func_w`) to control which
    points contribute to the central-value estimate at each iteration of
    the RCR rejection loop.

    The `indices` attribute records the post-`mu_func` index list; the
    rejection loop reads it back after each call.
    """

    def __init__(self):
        self.indices: np.ndarray = np.empty(0, dtype=np.int64)

    def mu_func(self, flags: np.ndarray, y: np.ndarray) -> tuple[np.ndarray, np.ndarray]:
        """Return (indices, true_y) given current flags and the raw y array.

        Default behavior mirrors the C++ no-op base case: keep all flagged
        points. Subclasses override for non-trivial filtering.
        """
        idx = np.where(flags)[0].astype(np.int64)
        self.indices = idx
        return idx, y[idx].copy()

    def mu_func_w(
        self, flags: np.ndarray, w: np.ndarray, y: np.ndarray
    ) -> tuple[np.ndarray, np.ndarray, np.ndarray]:
        """Weighted twin of `mu_func`. Returns (indices, true_w, true_y)."""
        idx = np.where(flags)[0].astype(np.int64)
        self.indices = idx
        return idx, w[idx].copy(), y[idx].copy()

# ============================================================================
# Module: functional.py
# ============================================================================




# C++-oracle constants (FunctionalForm.cpp:903-904). When combo count > 40000
# the C++ samples 20000 weighted draws instead of using all combos. The port
# mirrors these numbers exactly for bit-identical parity.
_COMBO_LIMIT = 20000        # number of draws when sampling triggers
_COMBO_FRAC = 0.5           # trigger threshold: sample iff frac*total > limit
# Nonlinear path can't afford 40000 fsolve calls — cap it tighter. Hits
# performance, not parity, since nonlinear models rarely produce enough
# kept points to exceed this anyway.
_COMBO_SAMPLE_CAP_NONLINEAR = 500


def _cpp_mt19937_seed_state(seed: int) -> np.ndarray:
    """C++11 §26.5.3.1 seed expansion for std::mt19937.

    Returns the 624-element uint32 state array as produced by
    std::mt19937(seed), so it can be injected into numpy's MT19937
    bit-generator (numpy uses a different seed expansion by default).
    """
    state = np.empty(624, dtype=np.uint32)
    state[0] = seed & 0xFFFFFFFF
    for i in range(1, 624):
        prev = int(state[i - 1])
        state[i] = (1812433253 * (prev ^ (prev >> 30)) + i) & 0xFFFFFFFF
    return state


def _cpp_mt19937_uint32_stream(seed: int, n: int) -> np.ndarray:
    """Return `n` uint32 values matching std::mt19937(seed).

    Uses numpy's compiled MT19937 BitGenerator with C++-style state
    injection so the bit sequence matches the C++ oracle exactly.
    Empirically, numpy's `random_raw` for MT19937 emits one 32-bit value
    per call placed in the low 32 bits of a uint64 (high bits zero), so
    we just take the low 32 bits.
    """
    bg = np.random.MT19937(0)  # seed ignored — we overwrite state below
    bg.state = {
        'bit_generator': 'MT19937',
        'state': {'key': _cpp_mt19937_seed_state(seed), 'pos': 624},
    }
    raws64 = bg.random_raw(n)
    return raws64.astype(np.uint32)

# The C++ uses DBL_MIN as a sentinel "tiny but nonzero" weight when an
# uncertainty-derived weight would be NaN, inf, or zero. numpy's
# equivalent is np.finfo(np.float64).tiny.
_TINY = float(np.finfo(np.float64).tiny)


def _half_sample_bounds(sorted_arr: np.ndarray,
                        sorted_w: np.ndarray) -> tuple[int, int]:
    """Return (low_idx, high_idx) of the half-sample mode window in a
    weighted, sorted array. Direct numpy translation of the C++
    `getHalfSampleBounds` (cpp/src/RCR.cpp:990) — find the index pair
    bracketing half the total weight, minimizing window WIDTH."""
    n = sorted_arr.size
    if n <= 1:
        return 0, max(n - 1, 0)
    cumw = np.cumsum(sorted_w)
    s_vec = cumw - 0.5 * sorted_w
    half = float(cumw[-1]) * 0.5
    totals = s_vec + half
    ks = np.searchsorted(s_vec, totals, side="right") - 1
    valid = (s_vec <= half) & (totals <= s_vec[-1] * (1 + 1e-12) + 1e-12)
    if not np.any(valid):
        return 0, n - 1
    widths = np.where(valid, sorted_arr[np.clip(ks, 0, n - 1)] - sorted_arr, np.inf)
    min_idx = int(np.argmin(widths))
    return min_idx, int(ks[min_idx])


class PriorType(Enum):
    """Match the C++ `priorTypes` enum in cpp/src/FunctionalForm.h."""
    CUSTOM = "CUSTOM_PRIORS"
    GAUSSIAN = "GAUSSIAN_PRIORS"
    CONSTRAINED = "CONSTRAINED_PRIORS"
    MIXED = "MIXED_PRIORS"


class Priors:
    """Probabilistic priors on model parameters.

    Mirrors the C++ `Priors` class (cpp/src/FunctionalForm.h:22). Four
    modes:

    - ``GAUSSIAN``: `gaussian_params[i] = (mu_i, sigma_i)`. NaN values mean
      no prior on that param. Implemented as quadratic penalty residuals
      `(params[i] - mu_i) / sigma_i` appended to the least-squares residual.
    - ``CONSTRAINED``: `param_bounds[i] = (lb_i, ub_i)`. NaN means
      unbounded on that side. Wired into scipy.optimize.least_squares as
      its `bounds=` argument.
    - ``MIXED``: both Gaussian and Constrained simultaneously.
    - ``CUSTOM``: user supplies `p(params) -> ndarray` which is appended
      to the residual vector verbatim (allows arbitrary log-posterior
      shape).

    Construct with the appropriate mode + data; the regression() routine
    in FunctionalForm reads `prior_type`, `gaussian_params`,
    `param_bounds`, and `p` as needed.
    """

    def __init__(
        self,
        prior_type: PriorType | None = None,
        p: Callable[[np.ndarray], np.ndarray] | None = None,
        gaussian_params: Sequence[Sequence[float]] | None = None,
        param_bounds: Sequence[Sequence[float]] | None = None,
    ):
        self.prior_type = prior_type
        self.p = p
        self.gaussian_params = (None if gaussian_params is None
                                else np.asarray(gaussian_params, dtype=np.float64))
        self.param_bounds = (None if param_bounds is None
                             else np.asarray(param_bounds, dtype=np.float64))

    def gaussian_residuals(self, params: np.ndarray) -> np.ndarray:
        """For active Gaussian priors, return `(params[i] - mu) / sigma`
        for each param with a defined (non-NaN) prior. Empty array if no
        Gaussian priors active."""
        if (self.prior_type not in (PriorType.GAUSSIAN, PriorType.MIXED)
                or self.gaussian_params is None):
            return np.empty(0, dtype=np.float64)
        gp = self.gaussian_params
        out: list[float] = []
        for i in range(min(params.size, gp.shape[0])):
            mu, sigma = float(gp[i, 0]), float(gp[i, 1])
            if not (np.isnan(mu) or np.isnan(sigma)) and sigma > 0:
                out.append((float(params[i]) - mu) / sigma)
        return np.asarray(out, dtype=np.float64)

    def scipy_bounds(self, n_params: int) -> tuple[np.ndarray, np.ndarray]:
        """Convert `param_bounds` into the (low, high) arrays scipy expects
        (with `np.inf` substituted for NaN — i.e. unbounded). For Gaussian-
        or no-prior cases, returns the trivial all-unbounded pair."""
        low = np.full(n_params, -np.inf, dtype=np.float64)
        high = np.full(n_params, np.inf, dtype=np.float64)
        if (self.prior_type not in (PriorType.CONSTRAINED, PriorType.MIXED)
                or self.param_bounds is None):
            return low, high
        pb = self.param_bounds
        for i in range(min(n_params, pb.shape[0])):
            lb, ub = float(pb[i, 0]), float(pb[i, 1])
            if not np.isnan(lb):
                low[i] = lb
            if not np.isnan(ub):
                high[i] = ub
        return low, high


@dataclass
class FunctionalFormResults:
    parameters: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    parameter_uncertainties: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    pivot: float = 0.0
    pivot_ND: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))


class FunctionalForm:
    """Parametric model for RCR.

    Phase 2 minimal constructor: ``FunctionalForm(f, xdata, ydata, partials,
    guess, tol=1e-6, weights=None, error_y=None)``. Matches the simplest
    of the C++'s 16 overloads (1D, optional weights, optional error bars,
    no priors, no pivot search).

    Use with::

        model = FunctionalForm(linear, xdata, ydata, [d1, d2], guess)
        r = RCR(RejectionTech.LS_MODE_68)
        r.set_parametric_model(model)
        r.perform_rejection(ydata)
        best = model.result.parameters
    """

    # Static-on-class pivot, mirroring the C++ `static double FunctionalForm::pivot`
    # so that user-supplied model functions can reference
    # `FunctionalForm.pivot` directly inside f(x, params).
    pivot: float = 0.0
    pivot_ND: np.ndarray = np.empty(0, dtype=np.float64)

    def __init__(
        self,
        f: Callable[[float, Sequence[float]], float],
        xdata: Sequence[float],
        ydata: Sequence[float],
        partials: Sequence[Callable[[float, Sequence[float]], float]],
        guess: Sequence[float],
        tol: float = 1e-6,
        weights: Sequence[float] | None = None,
        error_y: Sequence[float] | None = None,
        priors: "Priors | None" = None,
        pivot_function: Callable[
            [np.ndarray, np.ndarray,
             Callable[[float, Sequence[float]], float], np.ndarray],
            float,
        ] | None = None,
        pivot_guess: float | None = None,
    ):
        self.f = f
        self.partialsvector = list(partials)
        x_arr = np.asarray(xdata, dtype=np.float64)
        # ND detection: 2D xdata (N rows, D cols) routes through the ND
        # code path; 1D xdata is the standard 1D-in-x case.
        self.ND_check = x_arr.ndim == 2
        self.x = x_arr
        self.y = np.asarray(ydata, dtype=np.float64)
        self.guess = np.asarray(guess, dtype=np.float64)
        self.tolerance = tol
        self.M = self.guess.size      # number of model parameters
        # N = number of data POINTS (rows in x for ND, length for 1D).
        self.N = x_arr.shape[0]

        self.weighted_check = weights is not None
        self.w = (np.ones(self.N, dtype=np.float64) if weights is None
                  else np.asarray(weights, dtype=np.float64))
        self.has_error_bars = error_y is not None
        self.sigma_y = (None if error_y is None
                        else np.asarray(error_y, dtype=np.float64))

        # Mutable per-iteration state — set by set_true_vec().
        self.flags = np.ones(self.N, dtype=bool)
        self.indices = np.arange(self.N, dtype=np.int64)
        self.trueY = self.y.copy()
        self.trueW = self.w.copy()
        self.parameters = self.guess.copy()
        self.meanstartingpoint = self.guess.copy()

        # Detect whether the model is linear in its parameters by probing
        # the partials at two different parameter vectors. If the partials
        # don't depend on params (within float tolerance), we can solve
        # M-combinations of points via vectorized np.linalg.solve. Otherwise
        # we fall back to scipy.fsolve per combo.
        self._is_linear_in_params = self._probe_linear_in_params()

        # Filled lazily by build_model_space when MEDIAN/MODE mu_tech is
        # requested. Shape: parameterSpace[k] = 1D array of candidate
        # values for the k-th parameter across combos.
        self.parameterSpace: list[np.ndarray] = [np.empty(0) for _ in range(self.M)]
        self.weightSpace: list[np.ndarray] = [np.empty(0) for _ in range(self.M)]
        # Tracks the last mu_tech requested so we know whether to refresh
        # parameterSpace each iteration (MEDIAN/MODE need it, MEAN doesn't).
        self._last_mu_tech: str | None = None

        # Output bundle, populated each iteration by regression().
        self.result = FunctionalFormResults()
        self.result.parameters = self.guess.copy()

        # Priors
        self.priors = priors
        self.has_priors = priors is not None

        # Pivot search. If a pivot_function is provided, build_model_space
        # recomputes `FunctionalForm.pivot` each iteration from current
        # trueX/trueW/parameters. `pivot` is a class-level mirror of the C++
        # static `FunctionalForm::pivot`, so user model functions can read
        # it as a global. We reset it on each construction so state from a
        # previous instance (or test) doesn't leak into this one.
        self.pivot_function = pivot_function
        self.has_custom_pivot = pivot_function is not None
        type(self).pivot = float(pivot_guess) if pivot_guess is not None else 0.0
        type(self).pivot_ND = np.empty(0, dtype=np.float64)

    def _probe_linear_in_params(self) -> bool:
        """Heuristic: a model is linear in params iff each partial
        derivative is independent of the params vector. Test by evaluating
        each partial at SEVERAL x values with two different param vectors
        and comparing to float tolerance. Multiple x values are necessary
        because some nonlinear partials happen to be constant at x=0
        (e.g., exp(b*0) = 1 regardless of b).

        Used at construction; defaults to False (slow path) on any error.
        """
        try:
            # Probe at up to 5 distinct x values spread across the range.
            if self.x.size == 0:
                return False
            n_probe = min(5, self.x.size)
            if self.ND_check:
                idx = np.linspace(0, self.x.shape[0] - 1, n_probe, dtype=int)
                xs_probe = [self.x[i] for i in idx]
            else:
                idx = np.linspace(0, self.x.size - 1, n_probe, dtype=int)
                xs_probe = [float(self.x[i]) for i in idx]
            p1 = self.guess.copy()
            p2 = self.guess.copy() + 1.0
            for partial in self.partialsvector:
                for xp in xs_probe:
                    v1 = float(partial(xp, p1))
                    v2 = float(partial(xp, p2))
                    if not (np.isfinite(v1) and np.isfinite(v2)):
                        return False
                    if abs(v1 - v2) > 1e-10 * (abs(v1) + abs(v2) + 1.0):
                        return False
            return True
        except Exception:
            return False

    # -----------------------------------------------------------------
    # Per-iteration hooks called by the RCR rejection loop
    # -----------------------------------------------------------------

    def set_true_vec(self, flags: np.ndarray, y: np.ndarray,
                     w: np.ndarray | None = None) -> None:
        """Port of cpp/src/FunctionalForm.cpp:770 (and :735 weighted).
        Records the current flag mask + filtered y/w views; called once
        per rejection-loop iteration before regression()."""
        self.flags = flags
        self.indices = np.where(flags)[0].astype(np.int64)
        self.trueY = y[self.indices].copy()
        if w is not None:
            self.trueW = w[self.indices].copy()

    def build_model_space(self, build_combos: bool = False) -> None:
        """Port of cpp/src/FunctionalForm.cpp:799.

        Two responsibilities in the C++:
          1. Recompute the pivot point (if `has_custom_pivot`) from current
             flagged x / weights / parameters. Always done.
          2. Build parameterSpace / weightSpace from M-combinations of
             flagged data points, for use by get_*_median / get_*_mode.
             Only done when `build_combos=True` (caller passes this when
             mu_tech is MEDIAN or MODE).
        """
        if self.has_custom_pivot and self.pivot_function is not None:
            truex = self.x[self.indices]
            n_kept = truex.shape[0]
            if self.weighted_check:
                truew = self.w[self.indices]
            else:
                truew = np.ones(n_kept, dtype=np.float64)
            new_pivot = self.pivot_function(
                truex, truew, self.f, np.asarray(self.parameters, dtype=np.float64))
            if self.ND_check:
                arr = np.asarray(new_pivot, dtype=np.float64)
                type(self).pivot_ND = arr
                self.result.pivot_ND = arr
            else:
                p = float(new_pivot)
                type(self).pivot = p
                self.result.pivot = p

        if build_combos:
            self._build_param_combo_space()
        return None

    def _build_param_combo_space(self) -> None:
        """For each M-combination of currently-flagged data points, solve
        for the parameter vector that fits the model exactly through those
        M points; collect the resulting parameter values per dimension
        into self.parameterSpace and corresponding weights into
        self.weightSpace.

        When the number of combinations exceeds the C++'s trigger threshold
        (40000), we mirror the C++ oracle's biased weighted-roulette sampler
        exactly (cpp/src/FunctionalForm.cpp:903-957). The C++ uses a fixed
        MT19937 seed (post-determinism-fix); we use the same seed and the
        same selection arithmetic so the chosen combos are bit-identical.
        """
        idx = self.indices
        n_kept = idx.size
        M = self.M
        if n_kept < M:
            self.parameterSpace = [np.empty(0) for _ in range(M)]
            self.weightSpace = [np.empty(0) for _ in range(M)]
            return

        total = comb(n_kept, M)

        # Always enumerate combos first (matches C++ which builds the full
        # combos_indices vector before deciding whether to sample). For the
        # nonlinear path this is too expensive, so we keep the tighter cap
        # there and skip the C++-matching algorithm.
        if not self._is_linear_in_params or self.ND_check:
            if total <= _COMBO_SAMPLE_CAP_NONLINEAR:
                combos = np.array(list(itertools.combinations(range(n_kept), M)),
                                  dtype=np.int64)
            else:
                # Nonlinear fallback: numpy sampling (not bit-identical to C++).
                rng = np.random.default_rng(seed=0xC0FFEE)
                combos = self._sample_combos_numpy(rng, n_kept, M,
                                                   _COMBO_SAMPLE_CAP_NONLINEAR)
        else:
            # Linear path: match C++ behavior exactly.
            combos_all = np.array(list(itertools.combinations(range(n_kept), M)),
                                  dtype=np.int64)
            if _COMBO_FRAC * total > _COMBO_LIMIT:
                combos = self._sample_combos_cpp(combos_all)
            else:
                combos = combos_all

        n_combos = combos.shape[0]

        # Solve for params + compute paramuncertainty-based weights.
        # Port of cpp/src/FunctionalForm.cpp:1011-2200 (one of the 8 type
        # combinations). We do the full weight calculation per combo:
        #   weight[c, k] = correctivesum[c] / sigma_param[c, k]**2
        # falling back to _TINY * correctivesum on NaN/inf/0 (mirrors the
        # C++ DBL_MIN sentinel).
        if self._is_linear_in_params and not self.ND_check:
            params_per_combo, weights_per_combo = (
                self._solve_combos_linear_1d_weighted(combos))
        else:
            params_per_combo, weights_per_combo = (
                self._solve_combos_general_weighted(combos))

        # Filter to finite-param combos. Mirrors the C++ behavior of
        # discarding combos where the GN solver failed (M+2 sentinel).
        #
        # extraParameterSpace / extraWeightSpace deliberately NOT
        # implemented: the C++ uses these for combos where modifiedGN
        # detected a "runaway" parameter (returned M+1 values). Neither
        # np.linalg.solve nor scipy.optimize.fsolve produces an M+1
        # signal in our port — they either succeed cleanly or return
        # non-finite/error. We discard non-finite results entirely.
        finite = np.all(np.isfinite(params_per_combo), axis=1) & \
                 np.all(np.isfinite(weights_per_combo), axis=1)
        params_per_combo = params_per_combo[finite]
        weights_per_combo = weights_per_combo[finite]
        self.parameterSpace = [params_per_combo[:, k] for k in range(M)]
        self.weightSpace = [weights_per_combo[:, k] for k in range(M)]

    def _sample_combos_cpp(self, combos_all: np.ndarray) -> np.ndarray:
        """Mirror the C++ oracle's biased weighted-roulette combo sampler
        (cpp/src/FunctionalForm.cpp:903-957) exactly so the chosen combos
        are bit-identical between port and oracle.

        Algorithm (as written in the C++, including its non-standard
        selection condition `R < weights[k]` after subtracting weights[k]):

          1. combo_weights[k] = sum of point weights for points in combo k
          2. totalcomboweight = sum of combo_weights
          3. For j in 0..combolimit-1:
               R = (raw_uint32 / 2^32) * totalcomboweight
               for k in 0..n_combos-1:
                   R -= combo_weights[k]
                   if R < combo_weights[k]:
                       select combo k; break
          4. Deduplicate (set sorted lexicographically — matches std::set
             iteration order).

        The selection condition is equivalent to picking the first k where
        `R_initial < cumweights[k-1] + 2*weights[k]`. This biases the first
        combo (effectively 2x weight) and makes the last combo unreachable.
        We replicate the bias exactly because matching C++ requires it.
        """
        idx = self.indices
        if self.weighted_check:
            point_w = np.asarray(self.w[idx], dtype=np.float64)
        else:
            # Unweighted: C++ uses w=1.0 per point in this path.
            point_w = np.ones(idx.size, dtype=np.float64)

        # combo_weights[k] = point_w[combos_all[k, 0]] + ... + point_w[combos_all[k, M-1]]
        combo_weights = point_w[combos_all].sum(axis=1)
        total_w = float(combo_weights.sum())
        n_combos = combos_all.shape[0]

        # Thresholds for vectorized selection. The C++ inner loop selects
        # the first k where R_initial < threshold[k], where
        #   threshold[k] = sum(weights[0..k-1]) + 2*weights[k]
        #              = cumweights[k] + weights[k]      (cumweights inclusive)
        cumweights = np.cumsum(combo_weights)
        thresholds = cumweights + combo_weights

        # When thresholds are monotonically non-decreasing we can use
        # np.searchsorted. That holds iff weights[k+1] >= weights[k]/2 for
        # all k. In unweighted mode all combo_weights are equal (=M), so
        # always monotonic. In weighted mode it usually holds; we fall
        # back to a slow linear scan if it doesn't, to guarantee a
        # bit-identical match in all cases.
        is_monotonic = bool(np.all(np.diff(thresholds) >= 0))

        # Generate _COMBO_LIMIT uint32 draws from std::mt19937(0xC0FFEE).
        raws = _cpp_mt19937_uint32_stream(0xC0FFEE, _COMBO_LIMIT)
        # IEEE-754 double exactly represents 2^32 = 4294967296.
        rs = (raws.astype(np.float64) / 4294967296.0) * total_w

        if is_monotonic:
            # Smallest k such that thresholds[k] > R  →  searchsorted side='right'.
            # (side='right' returns insertion index just past equal values,
            # which equals the first index with thresholds[k] > R for R not
            # exactly equal to a threshold — float R make ties effectively
            # impossible.)
            ks = np.searchsorted(thresholds, rs, side='right')
            # Defensive clip; in theory ks < n_combos always because
            # thresholds[n_combos-1] = totalcomboweight + weights[n_combos-1]
            # > totalcomboweight > R.
            ks = np.minimum(ks, n_combos - 1)
        else:
            # Fallback: literal translation of the C++ inner loop.
            ks = np.empty(_COMBO_LIMIT, dtype=np.int64)
            cw = combo_weights  # local alias
            for j in range(_COMBO_LIMIT):
                R = float(rs[j])
                sel = n_combos - 1
                for k in range(n_combos):
                    R -= cw[k]
                    if R < cw[k]:
                        sel = k
                        break
                ks[j] = sel

        # Dedup, preserve lex-sorted order to mirror C++ std::set iteration.
        chosen: set[tuple[int, ...]] = set()
        combos_picked = combos_all[ks]
        for row in combos_picked:
            chosen.add(tuple(int(x) for x in row))
        return np.array(sorted(chosen), dtype=np.int64)

    def _sample_combos_numpy(self, rng: np.random.Generator,
                             n_kept: int, M: int, n_samples: int) -> np.ndarray:
        """Numpy-based combo sampler. Used only in the nonlinear-model path
        where matching C++ exactly is not worth the enumeration cost. Not
        bit-identical to the C++ oracle."""
        idx = self.indices
        if self.weighted_check:
            point_w = self.w[idx]
            point_w = point_w / point_w.sum()
        else:
            point_w = None
        chosen: set[tuple[int, ...]] = set()
        attempts = 0
        max_attempts = n_samples * 4
        while len(chosen) < n_samples and attempts < max_attempts:
            picks = rng.choice(n_kept, size=M, replace=False, p=point_w)
            chosen.add(tuple(sorted(int(p) for p in picks)))
            attempts += 1
        return np.array(sorted(chosen), dtype=np.int64)

    def _solve_combos_linear_1d(self, combos: np.ndarray) -> np.ndarray:
        """Vectorized solve for `linear-in-params` 1D models. For each
        combo, build the M×M Jacobian-of-params matrix `A` (partials
        evaluated at each x in the combo) and the M-vector y, then call
        np.linalg.solve to get the unique param vector through those M
        points. Shape: (n_combos, M)."""
        idx = self.indices
        n_combos = combos.shape[0]
        M = self.M
        x_kept = self.x[idx]
        y_kept = self.y[idx]
        params0 = self.parameters  # only used to provide shape to partials

        # A[c, a, b] = partials[b](x[combos[c, a]], params0)
        A = np.empty((n_combos, M, M), dtype=np.float64)
        for a in range(M):
            x_a = x_kept[combos[:, a]]
            for b in range(M):
                # partials are scalar functions; loop over n_combos.
                A[:, a, b] = np.array(
                    [float(self.partialsvector[b](float(xv), params0))
                     for xv in x_a],
                    dtype=np.float64,
                )
        # numpy's batched solve interprets the last two dims of b as
        # (M, N) — so for a single RHS per system we add a trailing axis.
        b_vec = y_kept[combos][:, :, None]  # (n_combos, M, 1)
        try:
            result = np.linalg.solve(A, b_vec).squeeze(-1)
        except np.linalg.LinAlgError:
            # Some combos may have singular A; solve per-combo with
            # least-squares fallback (lstsq returns a value even when
            # singular).
            result = np.empty((n_combos, M), dtype=np.float64)
            for c in range(n_combos):
                try:
                    result[c] = np.linalg.solve(A[c], b_vec[c, :, 0])
                except np.linalg.LinAlgError:
                    result[c] = np.full(M, np.nan)
        return result

    def _solve_combos_linear_1d_weighted(self, combos: np.ndarray
                                          ) -> tuple[np.ndarray, np.ndarray]:
        """Vectorized solve for linear-in-params 1D models that ALSO
        computes the per-combo paramuncertainty-derived weights.

        Port of cpp/src/FunctionalForm.cpp:1941 (and similar branches for
        weighted / has-error-bars cases). For each combo:
          1. Solve A @ p = y for the param vector p (exact fit).
          2. Compute A_inv = inv(A) — the M×M Jacobian inverse.
          3. Compute the effective per-point sigma vector `sig_y`:
                unweighted, no-EB:        sig_y = [1, 1, ..., 1]
                weighted, no-EB:          sig_y = sqrt(wbar / w_i)
                unweighted, has-EB:       sig_y = sigma_y_i
                weighted, has-EB:         sig_y = sqrt(wbar / w_i) * sigma_y_i
          4. sigma_params[k] = ||A_inv[k, :] * sig_y||₂
          5. correctivesum = sum over combo of:
                unweighted, no-EB:        1.0  (so correctivesum = M)
                weighted, no-EB:          w_i
                unweighted, has-EB:       1 / sigma_y_i²
                weighted, has-EB:         w_i / sigma_y_i²
          6. weight[k] = correctivesum / sigma_params[k]²
             (fallback _TINY * correctivesum on NaN/inf/0)

        Returns (params (n_combos, M), weights (n_combos, M)).
        """
        idx = self.indices
        n_combos = combos.shape[0]
        M = self.M
        x_kept = self.x[idx]
        y_kept = self.y[idx]
        w_kept = self.w[idx]
        sy_kept = self.sigma_y[idx] if self.has_error_bars else None
        params0 = self.parameters

        # Build A[c, a, b] = partials[b](x[combo[c, a]], params0)
        A = np.empty((n_combos, M, M), dtype=np.float64)
        for a in range(M):
            x_a = x_kept[combos[:, a]]
            for b in range(M):
                A[:, a, b] = np.array(
                    [float(self.partialsvector[b](float(xv), params0))
                     for xv in x_a],
                    dtype=np.float64,
                )

        b_vec = y_kept[combos][:, :, None]   # (n_combos, M, 1)
        try:
            params = np.linalg.solve(A, b_vec).squeeze(-1)  # (n_combos, M)
        except np.linalg.LinAlgError:
            params = np.empty((n_combos, M), dtype=np.float64)
            for c in range(n_combos):
                try:
                    params[c] = np.linalg.solve(A[c], b_vec[c, :, 0])
                except np.linalg.LinAlgError:
                    params[c] = np.full(M, np.nan)

        # A_inv shape: (n_combos, M, M)
        try:
            A_inv = np.linalg.inv(A)
        except np.linalg.LinAlgError:
            A_inv = np.empty((n_combos, M, M), dtype=np.float64)
            for c in range(n_combos):
                try:
                    A_inv[c] = np.linalg.inv(A[c])
                except np.linalg.LinAlgError:
                    A_inv[c] = np.full((M, M), np.nan)

        # Per-combo wbar (only used in weighted case).
        if self.weighted_check:
            wbar = float(np.mean(w_kept))
        else:
            wbar = 1.0

        # Per-point sig_y for each combo: shape (n_combos, M).
        if self.weighted_check and self.has_error_bars:
            w_c = w_kept[combos]
            sy_c = sy_kept[combos]
            sig_y = np.sqrt(wbar / w_c) * sy_c
        elif self.weighted_check:
            w_c = w_kept[combos]
            sig_y = np.sqrt(wbar / w_c)
        elif self.has_error_bars:
            sig_y = sy_kept[combos]
        else:
            sig_y = np.ones((n_combos, M), dtype=np.float64)

        # sigma_params[c, k] = sqrt(sum_j (A_inv[c, k, j] * sig_y[c, j])²)
        # = norm along j of (A_inv * sig_y[:, None, :])
        scaled = A_inv * sig_y[:, None, :]
        sigma_params_sq = np.sum(scaled * scaled, axis=2)  # (n_combos, M)
        sigma_params = np.sqrt(sigma_params_sq)

        # correctivesum per combo
        if self.weighted_check and self.has_error_bars:
            correctivesum = np.sum(w_kept[combos] / (sy_kept[combos] ** 2), axis=1)
        elif self.weighted_check:
            correctivesum = np.sum(w_kept[combos], axis=1)
        elif self.has_error_bars:
            correctivesum = np.sum(1.0 / (sy_kept[combos] ** 2), axis=1)
        else:
            correctivesum = np.full(n_combos, float(M), dtype=np.float64)

        # weight[c, k] = correctivesum[c] / sigma_params[c, k]²
        # Fallback: _TINY * correctivesum where the natural formula is
        # NaN / inf / 0 (matches C++ DBL_MIN sentinel).
        with np.errstate(divide="ignore", invalid="ignore"):
            testweight = 1.0 / (sigma_params * sigma_params)
        bad = ~np.isfinite(testweight) | (testweight == 0.0)
        weights = correctivesum[:, None] * testweight
        # Where testweight is bad, use _TINY * correctivesum.
        fallback = _TINY * correctivesum[:, None]
        weights = np.where(bad, fallback, weights)

        return params, weights

    def _solve_combos_general_weighted(self, combos: np.ndarray
                                        ) -> tuple[np.ndarray, np.ndarray]:
        """Per-combo fsolve + weight computation for non-linear-in-params
        or ND models. Slower than the linear path, but produces the same
        (params, weights) shape."""
        idx = self.indices
        n_combos = combos.shape[0]
        M = self.M
        x_kept = self.x[idx]
        y_kept = self.y[idx]
        w_kept = self.w[idx]
        sy_kept = self.sigma_y[idx] if self.has_error_bars else None
        params0 = self.parameters

        if self.weighted_check:
            wbar = float(np.mean(w_kept))
        else:
            wbar = 1.0

        params = np.empty((n_combos, M), dtype=np.float64)
        weights = np.empty((n_combos, M), dtype=np.float64)

        for c in range(n_combos):
            xs = x_kept[combos[c]]
            ys = y_kept[combos[c]]

            def residual(p, _xs=xs, _ys=ys):
                if self.ND_check:
                    return np.array(
                        [self.f(_xs[i], p) - _ys[i] for i in range(M)],
                        dtype=np.float64,
                    )
                return np.array(
                    [self.f(float(_xs[i]), p) - float(_ys[i]) for i in range(M)],
                    dtype=np.float64,
                )

            try:
                sol, info, ier, msg = fsolve(residual, params0, full_output=True)
                if ier != 1:
                    params[c] = np.full(M, np.nan)
                    weights[c] = np.full(M, np.nan)
                    continue
                params[c] = sol
            except Exception:
                params[c] = np.full(M, np.nan)
                weights[c] = np.full(M, np.nan)
                continue

            # Build Jacobian at the fitted params.
            J = np.empty((M, M), dtype=np.float64)
            for a in range(M):
                xa = xs[a] if self.ND_check else float(xs[a])
                for b in range(M):
                    J[a, b] = float(self.partialsvector[b](xa, sol))
            try:
                J_inv = np.linalg.inv(J)
            except np.linalg.LinAlgError:
                weights[c] = np.full(M, np.nan)
                continue

            # sig_y per point in combo
            if self.weighted_check and self.has_error_bars:
                sig_y = np.sqrt(wbar / w_kept[combos[c]]) * sy_kept[combos[c]]
                correctivesum = float(np.sum(
                    w_kept[combos[c]] / (sy_kept[combos[c]] ** 2)))
            elif self.weighted_check:
                sig_y = np.sqrt(wbar / w_kept[combos[c]])
                correctivesum = float(np.sum(w_kept[combos[c]]))
            elif self.has_error_bars:
                sig_y = sy_kept[combos[c]]
                correctivesum = float(np.sum(1.0 / (sy_kept[combos[c]] ** 2)))
            else:
                sig_y = np.ones(M, dtype=np.float64)
                correctivesum = float(M)

            scaled = J_inv * sig_y[None, :]
            sigma_params_sq = np.sum(scaled * scaled, axis=1)
            sigma_params = np.sqrt(sigma_params_sq)

            with np.errstate(divide="ignore", invalid="ignore"):
                testweight = 1.0 / (sigma_params * sigma_params)
            bad = ~np.isfinite(testweight) | (testweight == 0.0)
            wt = correctivesum * testweight
            wt = np.where(bad, _TINY * correctivesum, wt)
            weights[c] = wt

        return params, weights

    # Note: `_solve_combos_linear_1d` and `_solve_combos_general` (without
    # weights) have been superseded by their `_weighted` cousins above,
    # which compute paramuncertainty-derived weights inline. The unweighted
    # variants are kept here for backward compat with any caller that
    # wants params-only (no test does today).

    def regression(self) -> np.ndarray:
        """Fit the model to the currently-flagged data via least squares.

        Port of cpp/src/FunctionalForm.cpp:2382. The C++ dispatches to
        modifiedGN(f, partials, y, x, guess, tol, [w], [sigma_y]); we use
        scipy.optimize.least_squares which solves the same problem with
        a Trust Region Reflective algorithm by default.

        Stashes `_last_jacobian` and `_last_residual_count` for the
        uncertainty computation in `get_bestfit_errorbars`.
        """
        x_good = self.x[self.indices]
        y_good = self.y[self.indices]

        def residual(params):
            if self.ND_check:
                # x_good has shape (n_kept, D); iterate rows.
                model_y = np.array([self.f(x_good[i], params)
                                    for i in range(x_good.shape[0])],
                                   dtype=np.float64)
            else:
                model_y = np.array([self.f(xi, params) for xi in x_good],
                                   dtype=np.float64)
            r = y_good - model_y
            if self.has_error_bars:
                sigma = self.sigma_y[self.indices]
                r = r / sigma
            if self.weighted_check:
                w_good = self.w[self.indices]
                r = r * np.sqrt(w_good)
            # Priors: append Gaussian-prior penalty residuals (constrained
            # bounds are handled via scipy's bounds= argument below). Custom
            # priors append the user-defined penalty.
            if self.has_priors and self.priors is not None:
                extra: list[np.ndarray] = []
                g = self.priors.gaussian_residuals(np.asarray(params, dtype=np.float64))
                if g.size > 0:
                    extra.append(g)
                if (self.priors.prior_type is PriorType.CUSTOM
                        and self.priors.p is not None):
                    extra.append(np.asarray(
                        self.priors.p(np.asarray(params, dtype=np.float64)),
                        dtype=np.float64,
                    ))
                if extra:
                    r = np.concatenate([r] + extra)
            return r

        # Constrained priors → scipy `bounds` argument.
        if (self.has_priors and self.priors is not None
                and self.priors.prior_type in (PriorType.CONSTRAINED, PriorType.MIXED)):
            low, high = self.priors.scipy_bounds(self.M)
            bounds = (low, high)
            # Clip initial guess into bounds (scipy errors otherwise).
            start = np.clip(self.meanstartingpoint, low, high)
        else:
            bounds = (-np.inf, np.inf)
            start = self.meanstartingpoint

        result = least_squares(
            residual,
            start,
            xtol=self.tolerance,
            ftol=self.tolerance,
            method="trf",
            bounds=bounds,
        )
        params = result.x.astype(np.float64)
        if params.size > self.M:
            params = params[: self.M]
        self.parameters = params
        # Stash for paramuncertainty:
        self._last_jacobian = np.asarray(result.jac, dtype=np.float64)
        self._last_residual = np.asarray(result.fun, dtype=np.float64)
        self._last_n_kept = x_good.size
        return params

    # -----------------------------------------------------------------
    # Robust parameter-space estimators: median / mode over combo space
    # -----------------------------------------------------------------

    def get_nd_median(self) -> np.ndarray:
        """Port of cpp/src/RCR.cpp:1142 (get2DMedian) and its 3D/ND
        cousins. Returns the per-parameter weighted median across the
        current parameterSpace. Stores the result as the new
        meanstartingpoint for the subsequent generalized-mean call.
        """
        params = np.empty(self.M, dtype=np.float64)
        for k in range(self.M):
            arr = self.parameterSpace[k]
            w_arr = self.weightSpace[k]
            if arr.size == 0:
                params[k] = self.parameters[k]
                continue
            idx = np.argsort(arr, kind="stable")
            sorted_x = arr[idx]
            sorted_w = w_arr[idx]
            params[k] = getMedian_w(sorted_w, sorted_x)
        self.meanstartingpoint = params.copy()
        return params

    def get_nd_mode(self) -> np.ndarray:
        """Port of cpp/src/RCR.cpp:1063 (get2DMode) and its 3D/ND cousins.
        Iteratively narrows each parameter dimension to its half-sample
        modal window, filtering combos to those within ALL dimensions'
        windows, until the windows stabilize. Returns the weighted median
        of the final filtered combos per parameter."""
        if any(arr.size == 0 for arr in self.parameterSpace):
            return self.parameters.copy()

        # All-combo arrays (mutable working set).
        x_dims = [arr.copy() for arr in self.parameterSpace]
        w_dims = [arr.copy() for arr in self.weightSpace]

        prev_signature = None
        for _ in range(100):  # safety cap
            # Find half-sample modal window per dimension.
            lows = np.empty(self.M, dtype=np.float64)
            highs = np.empty(self.M, dtype=np.float64)
            for k in range(self.M):
                arr = x_dims[k]
                if arr.size < 2:
                    lows[k] = -np.inf
                    highs[k] = np.inf
                    continue
                idx = np.argsort(arr, kind="stable")
                sorted_arr = arr[idx]
                sorted_w = w_dims[k][idx]
                low_idx, high_idx = _half_sample_bounds(sorted_arr, sorted_w)
                lows[k] = sorted_arr[low_idx]
                highs[k] = sorted_arr[high_idx]

            # Filter to combos within every dimension's window.
            mask = np.ones(self.parameterSpace[0].size, dtype=bool)
            for k in range(self.M):
                arr = self.parameterSpace[k]
                mask &= (arr >= lows[k]) & (arr <= highs[k])
            n_in_window = int(mask.sum())
            if n_in_window == 0:
                # Empty filter — keep previous working set, terminate.
                break
            new_x_dims = [self.parameterSpace[k][mask] for k in range(self.M)]
            new_w_dims = [self.weightSpace[k][mask] for k in range(self.M)]

            sig = (n_in_window, tuple(float(lows[k]) for k in range(self.M)),
                                tuple(float(highs[k]) for k in range(self.M)))
            if sig == prev_signature:
                break
            prev_signature = sig
            x_dims = new_x_dims
            w_dims = new_w_dims

        # Weighted median of the final filtered set per dimension.
        params = np.empty(self.M, dtype=np.float64)
        for k in range(self.M):
            arr = x_dims[k]
            w_arr = w_dims[k]
            if arr.size == 0:
                params[k] = self.parameters[k]
                continue
            idx = np.argsort(arr, kind="stable")
            sorted_x = arr[idx]
            sorted_w = w_arr[idx]
            params[k] = getMedian_w(sorted_w, sorted_x)
        self.meanstartingpoint = params.copy()
        return params

    def get_errors(self, line: np.ndarray) -> np.ndarray:
        """Port of cpp/src/FunctionalForm.cpp:2458 (and :2479 for ND).
        Returns y - f(x, line) for currently-flagged points only."""
        line = np.asarray(line, dtype=np.float64)
        if self.ND_check:
            return np.array(
                [self.y[i] - self.f(self.x[i], line)
                 for i in range(self.N) if self.flags[i]],
                dtype=np.float64,
            )
        return np.array(
            [self.y[i] - self.f(self.x[i], line) for i in range(self.N) if self.flags[i]],
            dtype=np.float64,
        )

    def get_bestfit_errorbars(self, line: np.ndarray) -> np.ndarray:
        """Parameter standard errors from the post-fit Jacobian.

        Port of cpp/src/FunctionalForm.cpp:2311, but using the standard
        nonlinear-least-squares covariance estimator
            cov = inv(J^T J) * (sum_res² / (N - M))
            unc[k] = sqrt(diag(cov)[k])
        applied to the scaled Jacobian that scipy.optimize.least_squares
        returns. The C++ uses an unusual reduction in `paramuncertainty`
        that operates on only M of the N weight/sigma_y values (looks
        like an indexing bug); we substitute the textbook formula.

        Returns an empty array if regression() hasn't been called yet
        (mirroring the C++ behavior when no error bars/weights are set).
        """
        if not hasattr(self, "_last_jacobian"):
            return np.empty(0, dtype=np.float64)
        J = self._last_jacobian
        r = self._last_residual
        n = self._last_n_kept
        m = self.M
        if n <= m:
            # under-determined; can't compute meaningful errors
            return np.full(m, np.nan, dtype=np.float64)
        # The Jacobian scipy returns is already scaled by sqrt(w) and 1/sigma_y
        # if those were folded into the residual function — so the standard
        # formula gives the correctly-weighted parameter covariance.
        JtJ = J.T @ J
        try:
            cov_unscaled = np.linalg.inv(JtJ)
        except np.linalg.LinAlgError:
            return np.full(m, np.nan, dtype=np.float64)
        # If weights/errors were supplied, the residuals are already
        # whitened (unitless), so we don't rescale; otherwise scale by
        # the residual variance.
        if self.has_error_bars or self.weighted_check:
            cov = cov_unscaled
        else:
            sigma_sq = float(np.sum(r * r)) / (n - m)
            cov = cov_unscaled * sigma_sq
        diag = np.diag(cov)
        # Numerical near-zero negatives can appear; clamp.
        diag = np.where(diag < 0, 0.0, diag)
        return np.sqrt(diag).astype(np.float64)

    # -----------------------------------------------------------------
    # The combined "mu" step the loop calls when muType == PARAMETRIC.
    # Mirrors RCR::handleMuTechSelect() (no-arg version) at
    # cpp/src/RCR.cpp:5937.
    # -----------------------------------------------------------------

    def handle_mu_tech_select(self, mu_tech: str = "MEAN") -> np.ndarray:
        """Fit the model and return residuals. Dispatches by mu_tech:
            MEAN   → regression() (standard nonlinear least squares)
            MEDIAN → weighted median across the M-combination parameter space
            MODE   → half-sample mode across the M-combination parameter space

        The rejection loop calls this once per iteration. Ports
        cpp/src/RCR.cpp:5937 (handleMuTechSelect, no-arg version).
        """
        if mu_tech == "MEDIAN":
            # build_model_space must have been called with build_combos=True
            line = self.get_nd_median()
        elif mu_tech == "MODE":
            line = self.get_nd_mode()
        else:  # MEAN or anything else
            line = self.regression()
        self.parameters = line
        self.result.parameters = line
        # Uncertainty is meaningful only after a least-squares fit; for
        # MEDIAN/MODE we still call get_bestfit_errorbars which will use
        # the last-stored Jacobian if regression() was ever called.
        self.result.parameter_uncertainties = self.get_bestfit_errorbars(line)
        return self.get_errors(line)

# ============================================================================
# Module: rejection.py
# ============================================================================






# C++ enum equivalents used internally by the loop. The public-facing
# RejectionTech (in api.py) drives `alignTechniques`.

class MuTech(Enum):
    MEAN = "MEAN"
    MEDIAN = "MEDIAN"
    MODE = "MODE"


class SigmaTech(Enum):
    STANDARD_DEVIATION = "STANDARD_DEVIATION"
    SIXTY_EIGHTH_PERCENTILE = "SIXTY_EIGHTH_PERCENTILE"
    DOUBLE_LINE = "DOUBLE_LINE"


class SigmaChoice(Enum):
    SINGLE = "SINGLE"
    LOWER = "LOWER"
    EACH = "EACH"


# Floor used in each-sigma rejection loops when a side's sigma collapses
# to 0 or negative. Mirrors the C++'s implicit "x/0 = inf → erfc(inf) = 0
# → reject" behavior: with sigma replaced by ~2.2e-308, the rejection
# ratio is astronomically large and the worst-residual point on that side
# gets rejected, identical to what the C++ does.
_SIGMA_FLOOR = float(np.finfo(np.float64).tiny)


# ---------------------------------------------------------------------------
# Forward declarations (the FN helpers reference each other via the dispatch
# inside fitDL; the actual bodies are below).
# ---------------------------------------------------------------------------


# ---------------------------------------------------------------------------
# Correction factor — n-dependent multiplier that turns stDev into sigma.
# ---------------------------------------------------------------------------

def getLower68CF(n: int) -> float:
    """Port of cpp/src/RCR.cpp:5244 — unweighted CF for LS_MODE_68."""
    if n < 101:
        return 1.0 / float(LS68UnityCF[n])
    return 1.0 / (1.0 - 2.3525 * n ** -0.627)


def getLower68CF_w(n: int, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5186 — weighted CF for LS_MODE_68. Piecewise
    polynomial in (log10 n, log10 getCFRatio(w))."""
    x = getCFRatio(w)
    if n < 101:
        y1 = 1.0 / float(LS68UnityCF[n])
    else:
        y1 = 1.0 / (1.0 - 2.3525 * n ** -0.627)
    if x == 0:
        return y1
    logx = math.log10(x)
    logn = math.log10(n)
    sign = -1 if n % 2 else 1  # pow(-1, n)
    if n == 2:
        b1, a1 = 1.08149771508934, -0.398375456223868
    elif n == 3:
        b1, a1 = 1.06994341034958, -1.14618991625901
    elif n == 4:
        return y1
    elif n == 5:
        b1, a1 = 0.45972894692595, -1.11957357644441
    elif 5 < n < 101:
        b1 = (-1.4528 * logn ** 3 + 5.3519 * logn ** 2 - 5.33 * logn + 2.2902
              + sign * 0.1879 * logn ** 0.9521)
        a1 = -1.1937 * logn ** 4 + 6.5268 * logn ** 3 - 13.308 * logn ** 2 + 11.432 * logn - 4.4769
    else:
        b1 = 1.4154 + sign * 0.363528
        a1 = -0.5408 * logn - 0.6482
    return y1 * 10 ** (10 ** (a1 + b1 * logx))


def getLowerDLCF(n: int) -> float:
    """Port of cpp/src/RCR.cpp:5320 — unweighted CF for LS_MODE_DL."""
    if n < 101:
        return 1.0 / float(LSDLUnityCF[n])
    return 1.0 / (1.0 - 3.3245 * n ** -0.65)


def getLowerDLCF_w(n: int, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5255 — weighted CF for LS_MODE_DL."""
    x = getCFRatio(w)
    if n < 101:
        y1 = 1.0 / float(LSDLUnityCF[n])
    else:
        y1 = 1.0 / (1.0 - 3.3245 * n ** -0.65)
    if x == 0:
        return y1
    logx = math.log10(x)
    logn = math.log10(n)
    sign = -1 if n % 2 else 1
    if n == 2:
        b1, a1 = 1.08149771508934, -0.398375456223868
    elif n == 3:
        b1, a1 = 1.51433669748424, -1.10939332689999
    elif n == 4:
        return y1
    elif n == 5:
        b1, a1 = 0.339404852185332, -1.1445790996528
    elif 5 < n < 21:
        b1 = (43.179 * logn ** 6 - 331.85 * logn ** 5 + 968.25 * logn ** 4
              - 1399.1 * logn ** 3 + 1070.7 * logn ** 2 - 415.81 * logn + 65.002
              + sign * 0.1365 * logn ** 2.4716)
        a1 = -0.2683 * logn ** 4 + 1.9174 * logn ** 3 - 5.062 * logn ** 2 + 5.452 * logn - 2.9999
    elif 20 < n < 101:
        b1 = 1.5144 * logn - 0.0448 + sign * 0.1365 * logn ** 2.4716
        a1 = -0.2683 * logn ** 4 + 1.9174 * logn ** 3 - 5.062 * logn ** 2 + 5.452 * logn - 2.9999
    else:
        b1 = 2.988077 + sign * 0.753032
        a1 = -0.4282 * logn - 0.4412
    return y1 * 10 ** (10 ** (a1 + b1 * logx))


def getSingleDLCF(n: int) -> float:
    """Port of cpp/src/RCR.cpp:5386 — unweighted CF for SS_MEDIAN_DL."""
    if n < 101:
        return 1.0 / float(SSDLUnityCF[n])
    return 1.0 / (1.0 - 3.578 * n ** -0.942)


def getSingleDLCF_w(n: int, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5331 — weighted CF for SS_MEDIAN_DL."""
    x = getCFRatio(w)
    if n < 101:
        y1 = 1.0 / float(SSDLUnityCF[n])
    else:
        y1 = 1.0 / (1.0 - 3.578 * n ** -0.942)
    if x == 0:
        return y1
    logx = math.log10(x)
    logn = math.log10(n)
    if n == 2:
        b1, a1 = 0.273907084639124, -3.15279135630884
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 3:
        b1, a1 = 0.448654915529039, -1.19134294551807
        return y1 / 10 ** (10 ** (a1 + b1 * logx))
    if n == 4:
        b1, a1 = 3.38253309393705, -1.05087405984868
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 5:
        b1, a1 = 0.118507989164207, -1.41453721585464
        return y1 / 10 ** (10 ** (a1 + b1 * logx))
    b1 = 0.1196 * logn + 4.5073
    a1 = -0.7914 * logn + 0.0243
    return y1 * 10 ** (10 ** (a1 + b1 * logx))


def getSingleFN(n: int, x: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5511 — unweighted FN model for sigmaChoice=SINGLE."""
    if n < 1001:
        return float(SSUnity[n])
    return 39.2519 * n ** -0.7969 + 1.8688


def getSingleFN_w(n: int, x: np.ndarray, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5470 — weighted FN model for sigmaChoice=SINGLE."""
    ratio = getFNRatio(x, w)
    if n < 1001:
        y1 = float(SSUnity[n])
    else:
        y1 = getSingleFN(n, x)
    if ratio == 0:
        return y1
    logx = math.log10(ratio)
    logn = math.log10(n)
    if 3 < n < 8:
        a1 = float(SSConstants[0, n])
        b1 = float(SSConstants[1, n])
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if 7 < n < 1001:
        b1 = (-0.3556 * logn ** 6 + 3.7036 * logn ** 5 - 14.932 * logn ** 4
              + 29.176 * logn ** 3 - 28.81 * logn ** 2 + 14.397 * logn - 2.6451)
        sign = -1 if n % 2 else 1
        a1 = (0.2313 * logn ** 6 - 3.02 * logn ** 5 + 15.997 * logn ** 4
              - 43.713 * logn ** 3 + 64.629 * logn ** 2 - 49.976 * logn + 15.484
              + sign * 0.1513 * n ** -0.471)
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n > 1000:
        return y1
    return -999999.0


def getEachDLCF(n: int) -> float:
    """Port of cpp/src/RCR.cpp:5459 — unweighted CF for ES_MODE_DL."""
    if n < 101:
        return 1.0 / float(ESDLUnityCF[n])
    return 1.0 / (1.0 - 3.1666 * n ** -0.833)


def getEachDLCF_w(n: int, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5397 — weighted CF for ES_MODE_DL."""
    x = getCFRatio(w)
    if n < 101:
        y1 = 1.0 / float(ESDLUnityCF[n])
    else:
        y1 = 1.0 / (1.0 - 3.1666 * n ** -0.833)
    if x == 0:
        return y1
    logx = math.log10(x)
    logn = math.log10(n)
    if n == 2:
        b1, a1 = 0.733632602759432, -2.59506757852134
    elif n == 3:
        b1, a1 = 0.816017988131836, -0.854637214866955
    elif n == 4:
        b1, a1 = 1.16048439814909, -0.954253810365265
    elif 4 < n < 20:
        b1 = 4.0458 * logn ** 2 - 6.4354 * logn + 2.7667
        a1 = -1.3993 * logn ** 3 + 6.5746 * logn ** 2 - 9.8844 * logn + 2.8572
    elif 19 < n < 101:
        b1 = 1.7394 * logn - 1.0435
        a1 = -1.3993 * logn ** 3 + 6.5746 * logn ** 2 - 9.8844 * logn + 2.8572
    else:
        b1 = 1.4123 * logn - 0.3893
        a1 = -0.5989 * logn - 0.6097
    return y1 * 10 ** (10 ** (a1 + b1 * logx))


def getEachFN(n: int, x: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5660 — unweighted FN model for sigmaChoice=EACH."""
    if n < 1001:
        return float(ESUnity[n])
    return 1.2591 ** (n ** 0.2052)


def getEachFN_w(n: int, x: np.ndarray, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5600 — weighted FN model for sigmaChoice=EACH."""
    ratio = getFNRatio(x, w)
    if n < 1001:
        y1 = float(ESUnity[n])
    else:
        y1 = getEachFN(n, x)
    if ratio == 0:
        return y1
    logx = math.log10(ratio)
    logn = math.log10(n)
    if n == 5:
        b1, a1 = 2.13417275654528, -0.466431459550531
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 6:
        b1, a1 = 1.0196951775215, -0.312373723591738
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 7:
        b1, a1 = 0.579724747519776, -0.750190463040497
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if 7 < n < 1001:
        b1 = (5.8718 * logn ** 4 - 47.049 * logn ** 3 + 131.12 * logn ** 2
              - 150.24 * logn + 61.727)
        a1 = (3.1767 * logn ** 6 - 34.561 * logn ** 5 + 152.16 * logn ** 4
              - 347.96 * logn ** 3 + 435.59 * logn ** 2 - 282.57 * logn + 73.696)
        b2 = -2.7584 * logn ** 2 + 17.078 * logn - 24.602
        a2 = -1.8953 * logn ** 2 + 11.745 * logn - 19.36
        if n < 191 or (n < 306 and (a1 + b1 * logx > a2 + b2 * logx)):
            return y1 * 10 ** (10 ** (a1 + b1 * logx))
        return y1 / 10 ** (10 ** (a2 + b2 * logx))
    b1, a1 = 1.8064, -1.1827
    return y1 / 10 ** (10 ** (a1 + b1 * logx))


def getLowerFN(n: int, x: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5589 — unweighted FN model for sigmaChoice=LOWER."""
    if n < 1001:
        return float(LSUnity[n])
    return 1.3399 ** (n ** 0.1765)


def getLowerFN_w(n: int, x: np.ndarray, w: np.ndarray) -> float:
    """Port of cpp/src/RCR.cpp:5522 — weighted FN model for sigmaChoice=LOWER."""
    ratio = getFNRatio(x, w)
    if n < 1001:
        y1 = float(LSUnity[n])
    else:
        y1 = getLowerFN(n, x)
    if ratio == 0:
        return y1
    logx = math.log10(ratio)
    logn = math.log10(n)
    if n == 5:
        y1 = 36.8534
        b1, a1 = -0.300348560626506, -0.0828207791627729
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 6:
        b1, a1 = -0.244262599183892, -0.267502535686502
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 7:
        b1, a1 = -0.409677351330214, -0.558845927943435
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if n == 8:
        b1, a1 = -0.488354948027081, -0.889342857411619
        return y1 * 10 ** (10 ** (a1 + b1 * logx))
    if 8 < n < 1001:
        b1 = 0.1462 * logn ** 3.0 - 4.2139 * logn ** 2.0 + 14.366 * logn - 10.658
        a1 = (-0.541 * logn ** 5.0 + 4.6943 * logn ** 4.0 - 15.407 * logn ** 3.0
              + 21.875 * logn ** 2.0 - 11.211 * logn - 0.3798)
        b2 = 26.945 * logn ** 3.0 - 221.42 * logn ** 2.0 + 606.91 * logn - 553.89
        a2 = 18.149 * logn ** 3.0 - 149.27 * logn ** 2.0 + 410.15 * logn - 378.47
        if n <= 264 or (n < 568 and (a1 + b1 * logx > a2 + b2 * logx)):
            return y1 * 10 ** (10 ** (a1 + b1 * logx))
        return y1 / 10 ** (10 ** (a2 + b2 * logx))
    # n >= 1001
    b1 = 0.0424 * logn + 1.4479
    a1 = 0.3861 * logn - 2.5852
    return y1 / 10 ** (10 ** (a1 + b1 * logx))


# ---------------------------------------------------------------------------
# Mu and sigma dispatch — mirrors handleMuTechSelect / handleSigmaTechSelect.
# ---------------------------------------------------------------------------

def _mu(mu_tech: MuTech, trueY: np.ndarray) -> tuple[float, np.ndarray]:
    """Return (mu, possibly-sorted trueY). MEDIAN and MODE both sort in-place
    in the C++, which the caller's diff-loop then ignores (it uses original
    trueY order). We return both for clarity.
    """
    if mu_tech is MuTech.MEAN:
        return getMean(trueY), trueY
    if mu_tech is MuTech.MEDIAN:
        s = np.sort(trueY)
        return getMedian(s), s
    # MODE
    s = np.sort(trueY)
    return halfSampleMode(s), s


def _select_candidates(
    flags: np.ndarray, y: np.ndarray, mu_tech: "MuTech",
    non_parametric_model=None, parametric_model=None,
) -> tuple[np.ndarray, np.ndarray, float]:
    """Unweighted (indices, trueY, mu) selection for the iterative/bulk
    loop bodies. Handles all three mu_types (VALUE/NONPARAMETRIC/PARAMETRIC)
    in one place to keep the loops short."""
    if parametric_model is not None:
        parametric_model.set_true_vec(flags, y)
        mu_name = mu_tech.value if hasattr(mu_tech, "value") else str(mu_tech)
        needs_combos = mu_name in ("MEDIAN", "MODE")
        parametric_model.build_model_space(build_combos=needs_combos)
        trueY = parametric_model.handle_mu_tech_select(mu_tech=mu_name)
        return parametric_model.indices, trueY, 0.0
    if non_parametric_model is not None:
        indices, trueY = non_parametric_model.mu_func(flags, y)
        mu, _ = _mu(mu_tech, trueY)
        return indices, trueY, mu
    indices = np.where(flags)[0]
    trueY = y[indices]
    mu, _ = _mu(mu_tech, trueY)
    return indices, trueY, mu


def _select_candidates_w(
    flags: np.ndarray, w: np.ndarray, y: np.ndarray, mu_tech: "MuTech",
    non_parametric_model=None, parametric_model=None,
) -> tuple[np.ndarray, np.ndarray, np.ndarray, float]:
    """Weighted (indices, trueW, trueY, mu) selection."""
    if parametric_model is not None:
        parametric_model.set_true_vec(flags, y, w=w)
        mu_name = mu_tech.value if hasattr(mu_tech, "value") else str(mu_tech)
        needs_combos = mu_name in ("MEDIAN", "MODE")
        parametric_model.build_model_space(build_combos=needs_combos)
        trueY = parametric_model.handle_mu_tech_select(mu_tech=mu_name)
        return parametric_model.indices, parametric_model.trueW, trueY, 0.0
    if non_parametric_model is not None:
        indices, trueW, trueY = non_parametric_model.mu_func_w(flags, w, y)
        return indices, trueW, trueY, _mu_w(mu_tech, trueW, trueY)
    indices = np.where(flags)[0]
    return indices, w[indices], y[indices], _mu_w(mu_tech, w[indices], y[indices])


def _mu_w(mu_tech: MuTech, trueW: np.ndarray, trueY: np.ndarray) -> float:
    """Weighted mu dispatch — handleMuTechSelect(w, y) at cpp/src/RCR.cpp:5907.
    For MEDIAN and MODE the C++ does sort(w, y) (sort y ascending, w along)
    before getMedian/getMode. We do the same."""
    if mu_tech is MuTech.MEAN:
        return getMean_w(trueW, trueY)
    idx = np.argsort(trueY, kind="stable")
    w_s = trueW[idx]
    y_s = trueY[idx]
    if mu_tech is MuTech.MEDIAN:
        return getMedian_w(w_s, y_s)
    # MODE
    return halfSampleMode_w(w_s, y_s)


def _sigma_lower(sigma_tech: SigmaTech, w: np.ndarray, diff: np.ndarray,
                 delta: float, counter: int) -> float:
    """Port of handleSigmaTechSelect for sigmaChoice=LOWER. The delta passed
    in is the user-set delta (default 1.0); for LOWER, getStDev uses delta/2.

    For DOUBLE_LINE we always use the weighted helpers — multiplying by w=1.0
    is bit-exact under IEEE 754, so the unweighted loop (which passes w=ones)
    produces the same result as the C++ unweighted handleSigmaTechSelect.
    """
    if sigma_tech is SigmaTech.STANDARD_DEVIATION:
        return getStDev_w(delta / 2.0, w, diff)
    if sigma_tech is SigmaTech.SIXTY_EIGHTH_PERCENTILE:
        return get68th_w(w, diff)
    if sigma_tech is SigmaTech.DOUBLE_LINE:
        x = getXVec_w(diff.size, w)
        x_below = countAmountLessThanOne(x)
        if x_below > 2:
            return fitDL_w(counter, w, x, diff, getLowerFN_w)
        if x_below > 1:
            return fitSL_w(w, x, diff)
        return get68th_w(w, diff)
    raise ValueError(f"unsupported sigma_tech {sigma_tech!r}")


def _sigma_each(sigma_tech: SigmaTech, w: np.ndarray, diff: np.ndarray,
                delta: float, counter: int) -> float:
    """Sigma dispatch for sigmaChoice=EACH. Same as _sigma_lower except
    DOUBLE_LINE falls back via getEachFN_w."""
    if sigma_tech is SigmaTech.STANDARD_DEVIATION:
        return getStDev_w(delta / 2.0, w, diff)
    if sigma_tech is SigmaTech.SIXTY_EIGHTH_PERCENTILE:
        return get68th_w(w, diff)
    if sigma_tech is SigmaTech.DOUBLE_LINE:
        x = getXVec_w(diff.size, w)
        x_below = countAmountLessThanOne(x)
        if x_below > 2:
            return fitDL_w(counter, w, x, diff, getEachFN_w)
        if x_below > 1:
            return fitSL_w(w, x, diff)
        return get68th_w(w, diff)
    raise ValueError(f"unsupported sigma_tech {sigma_tech!r}")


def _sigma_single(sigma_tech: SigmaTech, w: np.ndarray, diff: np.ndarray,
                  delta: float, counter: int) -> float:
    """Sigma dispatch for sigmaChoice=SINGLE (used by iterativeSingleSigmaRCR).
    Differs from _sigma_lower: getStDev uses the FULL delta (not delta/2),
    and DOUBLE_LINE's fitDL falls back via getSingleFN instead of getLowerFN.
    """
    if sigma_tech is SigmaTech.STANDARD_DEVIATION:
        return getStDev_w(delta, w, diff)
    if sigma_tech is SigmaTech.SIXTY_EIGHTH_PERCENTILE:
        return get68th_w(w, diff)
    if sigma_tech is SigmaTech.DOUBLE_LINE:
        x = getXVec_w(diff.size, w)
        x_below = countAmountLessThanOne(x)
        if x_below > 2:
            return fitDL_w(counter, w, x, diff, getSingleFN_w)
        if x_below > 1:
            return fitSL_w(w, x, diff)
        return get68th_w(w, diff)
    raise ValueError(f"unsupported sigma_tech {sigma_tech!r}")


# ---------------------------------------------------------------------------
# Rejection test (Chauvenet criterion).
# ---------------------------------------------------------------------------

def _reject(true_count: int, max_index: int, ratio: float,
            flags: np.ndarray, y: np.ndarray) -> bool:
    """Port of cpp/src/RCR.cpp:5744. Returns True if rejection should STOP
    (no point rejected this iteration); False after marking flags[max_index]
    = False."""
    if distinctValuesCheck(flags, y) and true_count * erfcCustom(ratio) < 0.5:
        flags[max_index] = False
        return False
    return True


# ---------------------------------------------------------------------------
# The iterative lower-sigma loop.
# ---------------------------------------------------------------------------

def iterativeLowerSigmaRCR(
    y: np.ndarray,
    flags: np.ndarray,
    mu_tech: MuTech,
    sigma_tech: SigmaTech,
    delta: float,
    n_correct_fn,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Port of cpp/src/RCR.cpp:6408 — iterativeLowerSigmaRCR(y), unweighted.

    Mutates `flags` in place. Returns the per-iteration sigma/stdev/mu state
    that the caller can stash on RCRResults.

    n_correct_fn(n) returns the correction factor for the user's rejection
    technique; passed in so the loop doesn't have to know about RejectionTech.
    """
    mu = -1.0
    sigma = -1.0
    st_dev = -1.0
    st_dev_above = -1.0
    st_dev_below = -1.0

    stop = False
    while not stop:
        indices, trueY, mu = _select_candidates(
            flags, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        # Vectorized port of the C++ per-iteration walk (cpp/src/RCR.cpp:6461).
        # Computes |y-mu|, locates the worst residual, and splits the kept
        # set into below/above-mu lists. Ties at mu go to BOTH lists; only
        # the LAST tied point's position is reweighted to 0.5 (mirrors a
        # quirk in the C++ that is intentionally preserved for parity —
        # see rcrpy-porting-gotchas memory).
        diff = np.abs(trueY - mu)
        max_local = int(np.argmax(diff))
        max_val = float(diff[max_local])
        max_index = int(indices[max_local])

        is_eq = isEqual_vec_scalar(trueY, mu)
        # Order-preserving partition: keep_below covers (y<mu OR y==mu),
        # keep_above covers (y>mu OR y==mu). The C++ appends ties to BOTH
        # lists in trueY order; np.where preserves that ordering.
        keep_below = (trueY < mu) | is_eq
        keep_above = (trueY > mu) | is_eq
        diff_below = diff[keep_below]
        diff_above = diff[keep_above]
        w_below = np.ones(diff_below.size, dtype=np.float64)
        w_above = np.ones(diff_above.size, dtype=np.float64)
        split = bool(is_eq.any())
        if split:
            last_eq = int(np.where(is_eq)[0][-1])
            # Position of `last_eq` within keep_below (zero-indexed): the
            # number of True entries in keep_below at indices <= last_eq,
            # minus one.
            below_split_idx = int(np.sum(keep_below[: last_eq + 1])) - 1
            above_split_idx = int(np.sum(keep_above[: last_eq + 1])) - 1
            w_below[below_split_idx] = 0.5
            w_above[above_split_idx] = 0.5

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0

        # Sort each (w, diff) pair by diff ascending.
        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below = w_below[idx]
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above = w_above[idx]

        n_correction = n_correct_fn(true_count)

        if nonzero_above and nonzero_below:
            st_dev_above = _sigma_lower(sigma_tech, w_above, diff_above, delta, true_count)
            st_dev_below = _sigma_lower(sigma_tech, w_below, diff_below, delta, true_count)
            st_dev = min(st_dev_above, st_dev_below)
            sigma = st_dev * n_correction
        elif nonzero_above:
            st_dev = _sigma_lower(sigma_tech, w_above, diff_above, delta, true_count)
            st_dev_above = st_dev
            sigma = st_dev * n_correction
        elif nonzero_below:
            st_dev = _sigma_lower(sigma_tech, w_below, diff_below, delta, true_count)
            st_dev_below = st_dev
            sigma = st_dev * n_correction

        stop = _reject(true_count, max_index, max_val / sigma, flags, y)

    return {
        "mu": mu,
        "sigma": sigma,
        "st_dev": st_dev,
        "st_dev_above": st_dev_above,
        "st_dev_below": st_dev_below,
    }


# ---------------------------------------------------------------------------
# 3-pass refinement performed by performRejection.
# ---------------------------------------------------------------------------

def iterativeLowerSigmaRCR_w(
    w: np.ndarray,
    y: np.ndarray,
    flags: np.ndarray,
    mu_tech: MuTech,
    sigma_tech: SigmaTech,
    delta: float,
    n_correct_fn,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Weighted twin of iterativeLowerSigmaRCR. Port of cpp/src/RCR.cpp:6261.

    Note: unlike the unweighted port, the tie-at-mu split here is NOT buggy
    — the C++ pushes 0.5*trueW[i] into both wBelow and wAbove for EVERY
    tied point (vs. the unweighted version which only weights the LAST
    tie). We mirror that.
    """
    mu = -1.0
    sigma = -1.0
    st_dev = -1.0
    st_dev_above = -1.0
    st_dev_below = -1.0

    stop = False
    while not stop:
        indices, trueW, trueY, mu = _select_candidates_w(
            flags, w, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        # Vectorized weighted twin of the unweighted lower-sigma walk.
        # Ties at mu get 0.5*trueW[i] in BOTH lists (this is the "no LAST-
        # only bug" weighted variant — see cpp/src/RCR.cpp:6331+).
        diff = np.abs(trueY - mu)
        max_local = int(np.argmax(diff))
        max_val = float(diff[max_local])
        max_index = int(indices[max_local])

        is_eq = isEqual_vec_scalar(trueY, mu)
        keep_below = (trueY < mu) | is_eq
        keep_above = (trueY > mu) | is_eq
        diff_below = diff[keep_below]
        diff_above = diff[keep_above]
        # Weights: full trueW for below_only / above_only, 0.5*trueW for ties.
        w_below_full = np.where(is_eq, 0.5 * trueW, trueW)
        w_above_full = np.where(is_eq, 0.5 * trueW, trueW)
        w_below_arr = w_below_full[keep_below]
        w_above_arr = w_above_full[keep_above]

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0

        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below_arr = w_below_arr[idx]
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above_arr = w_above_arr[idx]

        n_correction = n_correct_fn(true_count, trueW)

        if nonzero_above and nonzero_below:
            st_dev_above = _sigma_lower(sigma_tech, w_above_arr, diff_above, delta, true_count)
            st_dev_below = _sigma_lower(sigma_tech, w_below_arr, diff_below, delta, true_count)
            st_dev = min(st_dev_above, st_dev_below)
            sigma = st_dev * n_correction
        elif nonzero_above:
            st_dev = _sigma_lower(sigma_tech, w_above_arr, diff_above, delta, true_count)
            st_dev_above = st_dev
            sigma = st_dev * n_correction
        elif nonzero_below:
            st_dev = _sigma_lower(sigma_tech, w_below_arr, diff_below, delta, true_count)
            st_dev_below = st_dev
            sigma = st_dev * n_correction

        stop = _reject(true_count, max_index, max_val / sigma, flags, y)

    return {
        "mu": mu,
        "sigma": sigma,
        "st_dev": st_dev,
        "st_dev_above": st_dev_above,
        "st_dev_below": st_dev_below,
    }


def iterativeEachSigmaRCR(
    y: np.ndarray,
    flags: np.ndarray,
    mu_tech: MuTech,
    sigma_tech: SigmaTech,
    delta: float,
    n_correct_fn,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Port of cpp/src/RCR.cpp:6707 — iterativeEachSigmaRCR(y), unweighted.

    Each-sigma differs from lower-sigma:
      - sigmaBelow and sigmaAbove are kept separate (no min).
      - The rejection-candidate `max` is in units of *its own side's sigma*
        (not max-of-diff / single-sigma).
      - Only mu / stDevBelow / stDevAbove / sigmaBelow / sigmaAbove get set
        on the result; sigma and stDev are left at their previous value.
    """
    mu = -1.0
    sigma_below = -1.0
    sigma_above = -1.0
    st_dev_above = -1.0
    st_dev_below = -1.0

    stop = False
    while not stop:
        indices, trueY, mu = _select_candidates(
            flags, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        # Vectorized each-sigma walk. Mirrors the unweighted lower-sigma
        # variant's tie handling (LAST tie position gets 0.5 weight),
        # except each-sigma doesn't track max here — that happens later
        # in the original-y side-aware max-ratio scan.
        diff = np.abs(trueY - mu)
        is_eq = isEqual_vec_scalar(trueY, mu)
        keep_below = (trueY < mu) | is_eq
        keep_above = (trueY > mu) | is_eq
        diff_below = diff[keep_below]
        diff_above = diff[keep_above]
        w_below = np.ones(diff_below.size, dtype=np.float64)
        w_above = np.ones(diff_above.size, dtype=np.float64)
        split = bool(is_eq.any())
        if split:
            last_eq = int(np.where(is_eq)[0][-1])
            below_split_idx = int(np.sum(keep_below[: last_eq + 1])) - 1
            above_split_idx = int(np.sum(keep_above[: last_eq + 1])) - 1
            w_below[below_split_idx] = 0.5
            w_above[above_split_idx] = 0.5

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0
        n_correction = n_correct_fn(true_count)

        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below = w_below[idx]
            st_dev_below = _sigma_each(sigma_tech, w_below, diff_below, delta, true_count)
            sigma_below = st_dev_below * n_correction
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above = w_above[idx]
            st_dev_above = _sigma_each(sigma_tech, w_above, diff_above, delta, true_count)
            sigma_above = st_dev_above * n_correction

        # Find max of |y-mu|/sigma over the original y, side-dependent.
        #
        # Two degenerate cases need handling distinctly:
        #   - EMPTY side (sigma_X stays at -1.0 sentinel because there were
        #     no points on that side this iteration): don't consider any
        #     candidates from this side.
        #   - DEGENERATE side (sigma_X collapsed to ≤ 0 even though the
        #     side had points — e.g., all residuals on that side were
        #     exactly equal): the C++ does FP `hold / 0 = +inf` and uses
        #     the rejection criterion erfc(inf) = 0 to reject the worst
        #     point. We mirror that with a tiny sigma floor so the ratio
        #     becomes effectively `inf` and the same point gets selected.
        #
        # `nonzero_below` / `nonzero_above` distinguish the two cases.
        max_val = -99999.0
        max_index = -1
        for i in range(y.size):
            if not flags[i]:
                continue
            yi = float(y[i])
            hold = abs(yi - mu)
            if yi < mu and nonzero_below:
                s = sigma_below if sigma_below > 0 else _SIGMA_FLOOR
                ratio = hold / s
                if ratio > max_val:
                    max_val = ratio
                    max_index = i
            if yi > mu and nonzero_above:
                s = sigma_above if sigma_above > 0 else _SIGMA_FLOOR
                ratio = hold / s
                if ratio > max_val:
                    max_val = ratio
                    max_index = i

        if max_index < 0:
            # No valid rejection candidate (e.g., all kept residuals
            # exactly at mu). The C++ here is technically undefined
            # behavior — `maxIndex` is uninitialized. We choose the safe
            # interpretation: terminate the loop.
            stop = True
        else:
            stop = _reject(true_count, max_index, max_val, flags, y)

    return {
        "mu": mu,
        "sigma_below": sigma_below,
        "sigma_above": sigma_above,
        "st_dev_above": st_dev_above,
        "st_dev_below": st_dev_below,
    }


def iterativeEachSigmaRCR_w(
    w: np.ndarray,
    y: np.ndarray,
    flags: np.ndarray,
    mu_tech: MuTech,
    sigma_tech: SigmaTech,
    delta: float,
    n_correct_fn,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Weighted twin of iterativeEachSigmaRCR. Port of cpp/src/RCR.cpp:6557.
    Tie-at-mu distributes 0.5*trueW[i] to both sides (no LAST-only bug)."""
    mu = -1.0
    sigma_below = -1.0
    sigma_above = -1.0
    st_dev_above = -1.0
    st_dev_below = -1.0

    stop = False
    while not stop:
        indices, trueW, trueY, mu = _select_candidates_w(
            flags, w, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        # Vectorized weighted each-sigma walk. Ties at mu get 0.5*trueW[i]
        # in BOTH lists (no LAST-only quirk for the weighted variant —
        # cpp/src/RCR.cpp:6557+).
        diff = np.abs(trueY - mu)
        is_eq = isEqual_vec_scalar(trueY, mu)
        keep_below = (trueY < mu) | is_eq
        keep_above = (trueY > mu) | is_eq
        diff_below = diff[keep_below]
        diff_above = diff[keep_above]
        w_below_full = np.where(is_eq, 0.5 * trueW, trueW)
        w_above_full = np.where(is_eq, 0.5 * trueW, trueW)
        w_below_arr = w_below_full[keep_below]
        w_above_arr = w_above_full[keep_above]

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0
        n_correction = n_correct_fn(true_count, trueW)

        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below_arr = w_below_arr[idx]
            st_dev_below = _sigma_each(sigma_tech, w_below_arr, diff_below, delta, true_count)
            sigma_below = st_dev_below * n_correction
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above_arr = w_above_arr[idx]
            st_dev_above = _sigma_each(sigma_tech, w_above_arr, diff_above, delta, true_count)
            sigma_above = st_dev_above * n_correction

        # See iterativeEachSigmaRCR for the rationale on the two-sentinel
        # split (empty side vs degenerate side).
        max_val = -99999.0
        max_index = -1
        for i in range(y.size):
            if not flags[i]:
                continue
            yi = float(y[i])
            hold = abs(yi - mu)
            if yi < mu and nonzero_below:
                s = sigma_below if sigma_below > 0 else _SIGMA_FLOOR
                ratio = hold / s
                if ratio > max_val:
                    max_val = ratio
                    max_index = i
            if yi > mu and nonzero_above:
                s = sigma_above if sigma_above > 0 else _SIGMA_FLOOR
                ratio = hold / s
                if ratio > max_val:
                    max_val = ratio
                    max_index = i

        if max_index < 0:
            stop = True
        else:
            stop = _reject(true_count, max_index, max_val, flags, y)

    return {
        "mu": mu,
        "sigma_below": sigma_below,
        "sigma_above": sigma_above,
        "st_dev_above": st_dev_above,
        "st_dev_below": st_dev_below,
    }


def iterativeSingleSigmaRCR(
    y: np.ndarray,
    flags: np.ndarray,
    mu_tech: MuTech,
    sigma_tech: SigmaTech,
    delta: float,
    n_correct_fn,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Port of cpp/src/RCR.cpp:6168 — iterativeSingleSigmaRCR(y), unweighted.

    Single-sigma: one diff array (just |y - mu|), one sigma, no below/above
    split. Used by SS_MEDIAN_DL.
    """
    mu = -1.0
    sigma = -1.0
    st_dev = -1.0
    stop = False
    while not stop:
        indices, trueY, mu = _select_candidates(
            flags, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff = np.abs(trueY - mu)
        max_local = int(np.argmax(diff))
        max_val = float(diff[max_local])
        max_index = int(indices[max_local])

        diff_sorted = np.sort(diff, kind="stable")
        w_ones = np.ones(diff_sorted.size, dtype=np.float64)
        st_dev = _sigma_single(sigma_tech, w_ones, diff_sorted, delta, true_count)
        sigma = st_dev * n_correct_fn(true_count)

        stop = _reject(true_count, max_index, max_val / sigma, flags, y)

    return {
        "mu": mu,
        "sigma": sigma,
        "st_dev": st_dev,
        "st_dev_above": -1.0,
        "st_dev_below": -1.0,
    }


def iterativeSingleSigmaRCR_w(
    w: np.ndarray,
    y: np.ndarray,
    flags: np.ndarray,
    mu_tech: MuTech,
    sigma_tech: SigmaTech,
    delta: float,
    n_correct_fn,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Port of cpp/src/RCR.cpp:6066 — weighted single-sigma loop.

    The C++ does `sort(trueW, diff)` — sorts diff ascending, takes trueW
    along. Mirror that with argsort(diff)."""
    mu = -1.0
    sigma = -1.0
    st_dev = -1.0
    stop = False
    while not stop:
        indices, trueW, trueY, mu = _select_candidates_w(
            flags, w, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff = np.abs(trueY - mu)
        max_local = int(np.argmax(diff))
        max_val = float(diff[max_local])
        max_index = int(indices[max_local])

        idx = np.argsort(diff, kind="stable")
        diff_sorted = diff[idx]
        w_sorted = trueW[idx]
        st_dev = _sigma_single(sigma_tech, w_sorted, diff_sorted, delta, true_count)
        sigma = st_dev * n_correct_fn(true_count, trueW)

        stop = _reject(true_count, max_index, max_val / sigma, flags, y)

    return {
        "mu": mu,
        "sigma": sigma,
        "st_dev": st_dev,
        "st_dev_above": -1.0,
        "st_dev_below": -1.0,
    }


def _bulk_sigma_lower(w: np.ndarray, diff: np.ndarray, counter: int) -> float:
    """Port of handleBulkSigmaTechSelect (LOWER) at cpp/src/RCR.cpp:6029.
    Bulk always uses DL/SL/68th cascade regardless of sigmaTech."""
    x = getXVec_w(diff.size, w)
    x_below = countAmountLessThanOne(x)
    if x_below > 2:
        return max(fitDL_w(counter, w, x, diff, getLowerFN_w),
                   fitSL_w(w, x, diff))
    if x_below > 1:
        return fitSL_w(w, x, diff)
    return get68th_w(w, diff)


def _bulk_reject(flags: np.ndarray, indices_sorted: np.ndarray,
                 ratios_sorted: np.ndarray, y: np.ndarray) -> bool:
    """Port of cpp/src/RCR.cpp:5768. Returns True if no point was rejected
    this pass (signal to stop). `ratios_sorted` is |y-mu|/sigma in ascending
    order, and `indices_sorted` is the original-y index at each position."""
    no_points_rejected = True
    size = ratios_sorted.size
    i = size - 1
    while i >= 0 and distinctValuesCheck(flags, y) \
            and size * erfcCustom(float(ratios_sorted[i])) < 0.5:
        flags[int(indices_sorted[i])] = False
        no_points_rejected = False
        i -= 1
    return no_points_rejected


def bulkLowerSigmaRCR(y: np.ndarray, flags: np.ndarray, mu_tech: MuTech,
                      n_correct_fn,
                      non_parametric_model=None,
                      parametric_model=None) -> dict:
    """Port of cpp/src/RCR.cpp:7173 — bulkLowerSigmaRCR(y), unweighted.

    Bulk rejection: one mu/sigma estimate per iteration, then reject ALL
    points failing the Chauvenet criterion in one pass.

    QUIRK: the C++ initializes `trueCount = y.size()` once OUTSIDE the
    while-loop and never updates it. Both handleBulkSigmaTechSelect and
    nCorrect therefore use the ORIGINAL sample size as `counter`, not the
    post-rejection kept-count. We mirror that for parity.
    """
    counter = int(y.size)  # stays pinned to original size, per C++ quirk
    mu = -1.0
    sigma = -1.0
    stop = False
    while not stop:
        kept, trueY, mu = _select_candidates(
            flags, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff_below_list: list[float] = []
        diff_above_list: list[float] = []
        diff_hold: list[float] = []
        below_split_idx = -1
        above_split_idx = -1
        split = False
        for i in range(true_count):
            yi = float(trueY[i])
            hold = getDiff(mu, yi)
            diff_hold.append(hold)
            if isEqual(yi, mu):
                diff_below_list.append(hold)
                diff_above_list.append(hold)
                split = True
                below_split_idx = len(diff_below_list) - 1
                above_split_idx = len(diff_above_list) - 1
            elif yi > mu:
                diff_above_list.append(hold)
            else:
                diff_below_list.append(hold)

        diff_below = np.array(diff_below_list, dtype=np.float64)
        diff_above = np.array(diff_above_list, dtype=np.float64)
        diff_hold_arr = np.array(diff_hold, dtype=np.float64)
        w_below = np.ones(diff_below.size, dtype=np.float64)
        w_above = np.ones(diff_above.size, dtype=np.float64)
        if split:
            w_below[below_split_idx] = 0.5
            w_above[above_split_idx] = 0.5

        # sort(indices, diffHold) — paired sort by diffHold ascending
        sort_idx = np.argsort(diff_hold_arr, kind="stable")
        indices_sorted = kept[sort_idx]
        diff_hold_sorted = diff_hold_arr[sort_idx]

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0
        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below = w_below[idx]
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above = w_above[idx]

        if nonzero_above and nonzero_below:
            sigma = min(_bulk_sigma_lower(w_below, diff_below, counter),
                        _bulk_sigma_lower(w_above, diff_above, counter)) * n_correct_fn(counter)
        elif nonzero_above:
            sigma = _bulk_sigma_lower(w_above, diff_above, counter) * n_correct_fn(counter)
        elif nonzero_below:
            sigma = _bulk_sigma_lower(w_below, diff_below, counter) * n_correct_fn(counter)

        ratios_sorted = diff_hold_sorted / sigma
        stop = _bulk_reject(flags, indices_sorted, ratios_sorted, y)

    return {"mu": mu, "sigma": sigma}


def bulkSingleSigmaRCR(y: np.ndarray, flags: np.ndarray, mu_tech: MuTech,
                       n_correct_fn,
                       non_parametric_model=None,
                       parametric_model=None) -> dict:
    """Port of cpp/src/RCR.cpp:6949 — single-sigma bulk, unweighted.

    Unlike bulkLowerSigmaRCR, this loop DOES update `trueCount` each
    iteration (line 7003 in C++). Single-sigma uses one diff array, no
    below/above split.
    """
    mu = -1.0
    sigma = -1.0
    stop = False
    while not stop:
        kept, trueY, mu = _select_candidates(
            flags, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff = np.abs(trueY - mu)
        diff_hold = diff.copy()
        diff_sorted = np.sort(diff, kind="stable")

        # `sort(indices, diffHold)` — sort indices by diffHold ascending
        sort_idx = np.argsort(diff_hold, kind="stable")
        indices_sorted = kept[sort_idx]
        diff_hold_sorted = diff_hold[sort_idx]

        # handleBulkSigmaTechSelect (single) — same DL/SL/68th cascade
        w_ones = np.ones(true_count, dtype=np.float64)
        sigma_raw = _bulk_sigma_lower(w_ones, diff_sorted, true_count)
        # ... but with getSingleFN inside fitDL — _bulk_sigma_lower uses
        # getLowerFN, so we need a single-sigma variant. Recompute here.
        x = getXVec_w(diff_sorted.size, w_ones)
        x_below = countAmountLessThanOne(x)
        if x_below > 2:
            sigma_raw = max(fitDL_w(true_count, w_ones, x, diff_sorted, getSingleFN_w),
                            fitSL_w(w_ones, x, diff_sorted))
        elif x_below > 1:
            sigma_raw = fitSL_w(w_ones, x, diff_sorted)
        else:
            sigma_raw = get68th_w(w_ones, diff_sorted)
        sigma = sigma_raw * n_correct_fn(true_count)

        ratios_sorted = diff_hold_sorted / sigma
        stop = _bulk_reject(flags, indices_sorted, ratios_sorted, y)

    return {"mu": mu, "sigma": sigma}


def bulkEachSigmaRCR(y: np.ndarray, flags: np.ndarray, mu_tech: MuTech,
                     n_correct_fn,
                     non_parametric_model=None,
                     parametric_model=None) -> dict:
    """Port of cpp/src/RCR.cpp:7445 — each-sigma bulk, unweighted.

    Each-sigma bulk: sigmaBelow and sigmaAbove computed separately, no min.
    The rejection ratio for each point is |y-mu| divided by ITS OWN side's
    sigma. Like single/lower bulks, this updates trueCount each iteration."""
    mu = -1.0
    sigma_below = -1.0
    sigma_above = -1.0
    stop = False
    while not stop:
        kept, trueY, mu = _select_candidates(
            flags, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff_below_list: list[float] = []
        diff_above_list: list[float] = []
        diff_hold: list[float] = []
        below_split_idx = -1
        above_split_idx = -1
        split = False
        for i in range(true_count):
            yi = float(trueY[i])
            hold = getDiff(mu, yi)
            diff_hold.append(hold)
            if isEqual(yi, mu):
                diff_below_list.append(hold)
                diff_above_list.append(hold)
                split = True
                below_split_idx = len(diff_below_list) - 1
                above_split_idx = len(diff_above_list) - 1
            elif yi > mu:
                diff_above_list.append(hold)
            else:
                diff_below_list.append(hold)

        diff_below = np.array(diff_below_list, dtype=np.float64)
        diff_above = np.array(diff_above_list, dtype=np.float64)
        diff_hold_arr = np.array(diff_hold, dtype=np.float64)
        w_below = np.ones(diff_below.size, dtype=np.float64)
        w_above = np.ones(diff_above.size, dtype=np.float64)
        if split:
            w_below[below_split_idx] = 0.5
            w_above[above_split_idx] = 0.5

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0

        n_correction = n_correct_fn(true_count)
        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below = w_below[idx]
            # handleBulkSigmaTechSelect (each shares the LOWER FN model)
            sigma_below = _bulk_sigma_lower_each(w_below, diff_below, true_count) * n_correction
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above = w_above[idx]
            sigma_above = _bulk_sigma_lower_each(w_above, diff_above, true_count) * n_correction

        # Normalize diffHold[i] by the appropriate side's sigma. Use the
        # tiny floor when a side's sigma collapsed (see _SIGMA_FLOOR
        # rationale above iterativeEachSigmaRCR).
        s_below = sigma_below if (nonzero_below and sigma_below > 0) else _SIGMA_FLOOR
        s_above = sigma_above if (nonzero_above and sigma_above > 0) else _SIGMA_FLOOR
        for i in range(diff_hold_arr.size):
            yi = float(y[int(kept[i])])
            if yi < mu:
                diff_hold_arr[i] = diff_hold_arr[i] / s_below
            elif yi > mu:
                diff_hold_arr[i] = diff_hold_arr[i] / s_above
            # equal-to-mu case: diff_hold is 0; matches C++ (which doesn't
            # divide for the equal case — neither yi<mu nor yi>mu fires).

        sort_idx = np.argsort(diff_hold_arr, kind="stable")
        indices_sorted = kept[sort_idx]
        ratios_sorted = diff_hold_arr[sort_idx]

        stop = _bulk_reject(flags, indices_sorted, ratios_sorted, y)

    return {"mu": mu, "sigma_below": sigma_below, "sigma_above": sigma_above}


def _bulk_sigma_lower_each(w: np.ndarray, diff: np.ndarray, counter: int) -> float:
    """handleBulkSigmaTechSelect for each-sigma context — uses getEachFN_w
    inside fitDL's fallback decision (vs getLowerFN_w for the lower-sigma
    bulk variant)."""
    x = getXVec_w(diff.size, w)
    x_below = countAmountLessThanOne(x)
    if x_below > 2:
        return max(fitDL_w(counter, w, x, diff, getEachFN_w),
                   fitSL_w(w, x, diff))
    if x_below > 1:
        return fitSL_w(w, x, diff)
    return get68th_w(w, diff)


def bulkLowerSigmaRCR_w(w: np.ndarray, y: np.ndarray, flags: np.ndarray,
                        mu_tech: MuTech, n_correct_fn_w,
                        non_parametric_model=None,
                        parametric_model=None) -> dict:
    """Port of cpp/src/RCR.cpp:7039. NOTE: unlike unweighted lower bulk,
    the weighted version updates trueCount each iteration (line 7090)."""
    mu = -1.0
    sigma = -1.0
    stop = False
    while not stop:
        kept, trueW, trueY, mu = _select_candidates_w(
            flags, w, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff_below_list: list[float] = []
        diff_above_list: list[float] = []
        w_below_list: list[float] = []
        w_above_list: list[float] = []
        diff_hold: list[float] = []
        for i in range(true_count):
            yi = float(trueY[i])
            wi = float(trueW[i])
            hold = getDiff(mu, yi)
            diff_hold.append(hold)
            if isEqual(yi, mu):
                diff_below_list.append(hold)
                diff_above_list.append(hold)
                w_below_list.append(0.5 * wi)
                w_above_list.append(0.5 * wi)
            elif yi > mu:
                diff_above_list.append(hold)
                w_above_list.append(wi)
            else:
                diff_below_list.append(hold)
                w_below_list.append(wi)

        diff_below = np.array(diff_below_list, dtype=np.float64)
        diff_above = np.array(diff_above_list, dtype=np.float64)
        w_below_arr = np.array(w_below_list, dtype=np.float64)
        w_above_arr = np.array(w_above_list, dtype=np.float64)
        diff_hold_arr = np.array(diff_hold, dtype=np.float64)

        sort_idx = np.argsort(diff_hold_arr, kind="stable")
        indices_sorted = kept[sort_idx]
        diff_hold_sorted = diff_hold_arr[sort_idx]

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0
        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below_arr = w_below_arr[idx]
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above_arr = w_above_arr[idx]

        if nonzero_above and nonzero_below:
            sigma = min(_bulk_sigma_lower(w_below_arr, diff_below, true_count),
                        _bulk_sigma_lower(w_above_arr, diff_above, true_count)) * n_correct_fn_w(true_count, trueW)
        elif nonzero_above:
            sigma = _bulk_sigma_lower(w_above_arr, diff_above, true_count) * n_correct_fn_w(true_count, trueW)
        elif nonzero_below:
            sigma = _bulk_sigma_lower(w_below_arr, diff_below, true_count) * n_correct_fn_w(true_count, trueW)

        ratios_sorted = diff_hold_sorted / sigma
        stop = _bulk_reject(flags, indices_sorted, ratios_sorted, y)

    return {"mu": mu, "sigma": sigma}


def bulkSingleSigmaRCR_w(w: np.ndarray, y: np.ndarray, flags: np.ndarray,
                         mu_tech: MuTech, n_correct_fn_w,
                         non_parametric_model=None,
                         parametric_model=None) -> dict:
    """Port of cpp/src/RCR.cpp:6855. Weighted single-sigma bulk.
    C++ does sort(trueW, diff) — sorts diff ascending with trueW along."""
    mu = -1.0
    sigma = -1.0
    stop = False
    while not stop:
        kept, trueW, trueY, mu = _select_candidates_w(
            flags, w, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff = np.abs(trueY - mu)
        diff_hold = diff.copy()

        # sort(trueW, diff) -- by diff ascending
        sort_idx_diff = np.argsort(diff, kind="stable")
        diff_sorted = diff[sort_idx_diff]
        trueW_sorted = trueW[sort_idx_diff]

        # sort(indices, diffHold) -- by diff_hold ascending
        sort_idx_hold = np.argsort(diff_hold, kind="stable")
        indices_sorted = kept[sort_idx_hold]
        diff_hold_sorted = diff_hold[sort_idx_hold]

        # handleBulkSigmaTechSelect (single) with weighted helpers
        x = getXVec_w(diff_sorted.size, trueW_sorted)
        x_below = countAmountLessThanOne(x)
        if x_below > 2:
            sigma_raw = max(fitDL_w(true_count, trueW_sorted, x, diff_sorted, getSingleFN_w),
                            fitSL_w(trueW_sorted, x, diff_sorted))
        elif x_below > 1:
            sigma_raw = fitSL_w(trueW_sorted, x, diff_sorted)
        else:
            sigma_raw = get68th_w(trueW_sorted, diff_sorted)
        sigma = sigma_raw * n_correct_fn_w(true_count, trueW)

        ratios_sorted = diff_hold_sorted / sigma
        stop = _bulk_reject(flags, indices_sorted, ratios_sorted, y)

    return {"mu": mu, "sigma": sigma}


def bulkEachSigmaRCR_w(w: np.ndarray, y: np.ndarray, flags: np.ndarray,
                       mu_tech: MuTech, n_correct_fn_w,
                       non_parametric_model=None,
                       parametric_model=None) -> dict:
    """Port of cpp/src/RCR.cpp:7301. Weighted each-sigma bulk."""
    mu = -1.0
    sigma_below = -1.0
    sigma_above = -1.0
    stop = False
    while not stop:
        kept, trueW, trueY, mu = _select_candidates_w(
            flags, w, y, mu_tech, non_parametric_model, parametric_model)
        true_count = trueY.size

        diff_below_list: list[float] = []
        diff_above_list: list[float] = []
        w_below_list: list[float] = []
        w_above_list: list[float] = []
        diff_hold: list[float] = []
        for i in range(true_count):
            yi = float(trueY[i])
            wi = float(trueW[i])
            hold = getDiff(mu, yi)
            diff_hold.append(hold)
            if isEqual(yi, mu):
                diff_below_list.append(hold)
                diff_above_list.append(hold)
                w_below_list.append(0.5 * wi)
                w_above_list.append(0.5 * wi)
            elif yi < mu:
                diff_below_list.append(hold)
                w_below_list.append(wi)
            else:
                diff_above_list.append(hold)
                w_above_list.append(wi)

        diff_below = np.array(diff_below_list, dtype=np.float64)
        diff_above = np.array(diff_above_list, dtype=np.float64)
        w_below_arr = np.array(w_below_list, dtype=np.float64)
        w_above_arr = np.array(w_above_list, dtype=np.float64)
        diff_hold_arr = np.array(diff_hold, dtype=np.float64)

        nonzero_above = diff_above.size > 0
        nonzero_below = diff_below.size > 0

        if nonzero_below:
            idx = np.argsort(diff_below, kind="stable")
            diff_below = diff_below[idx]
            w_below_arr = w_below_arr[idx]
            sigma_below = _bulk_sigma_lower_each(w_below_arr, diff_below, true_count) * n_correct_fn_w(true_count, trueW)
        if nonzero_above:
            idx = np.argsort(diff_above, kind="stable")
            diff_above = diff_above[idx]
            w_above_arr = w_above_arr[idx]
            sigma_above = _bulk_sigma_lower_each(w_above_arr, diff_above, true_count) * n_correct_fn_w(true_count, trueW)

        # Tiny floor when a side's sigma collapsed (see _SIGMA_FLOOR
        # rationale above iterativeEachSigmaRCR).
        s_below = sigma_below if (nonzero_below and sigma_below > 0) else _SIGMA_FLOOR
        s_above = sigma_above if (nonzero_above and sigma_above > 0) else _SIGMA_FLOOR
        for i in range(diff_hold_arr.size):
            yi = float(y[int(kept[i])])
            if yi < mu:
                diff_hold_arr[i] = diff_hold_arr[i] / s_below
            elif yi > mu:
                diff_hold_arr[i] = diff_hold_arr[i] / s_above

        sort_idx = np.argsort(diff_hold_arr, kind="stable")
        indices_sorted = kept[sort_idx]
        ratios_sorted = diff_hold_arr[sort_idx]
        stop = _bulk_reject(flags, indices_sorted, ratios_sorted, y)

    return {"mu": mu, "sigma_below": sigma_below, "sigma_above": sigma_above}


def _set_final_vectors_w(w: np.ndarray, y: np.ndarray, result_state: dict,
                         flags: np.ndarray, delta: float) -> dict:
    """Port of cpp/src/RCR.cpp:5079. Weighted setFinalVectors."""
    rejected_mask = ~flags
    rejected_y = y[rejected_mask].copy()
    rejected_w = w[rejected_mask].copy()
    clean_y = y[flags].copy()
    clean_w = w[flags].copy()
    mean_hold = getMean_w(clean_w, clean_y)

    cyb: list[float] = []
    cya: list[float] = []
    cwb: list[float] = []
    cwa: list[float] = []
    for yi, wi in zip(clean_y, clean_w):
        adj = float(yi) - mean_hold
        wf = float(wi)
        if isEqual(adj, 0.0):
            cya.append(adj)
            cyb.append(adj)
            cwb.append(0.5 * wf)
            cwa.append(0.5 * wf)
        elif adj > 0.0:
            cya.append(adj)
            cwa.append(wf)
        else:
            cyb.append(adj)
            cwb.append(wf)

    centered = clean_y - mean_hold
    st_dev_total = getStDev_w(delta, clean_w, centered)
    cyb_a = np.array(cyb, dtype=np.float64)
    cya_a = np.array(cya, dtype=np.float64)
    cwb_a = np.array(cwb, dtype=np.float64)
    cwa_a = np.array(cwa, dtype=np.float64)
    st_dev_below = getStDev_w(delta / 2.0, cwb_a, cyb_a) if cyb_a.size else -1.0
    st_dev_above = getStDev_w(delta / 2.0, cwa_a, cya_a) if cya_a.size else -1.0

    result_state["rejected_y"] = rejected_y
    result_state["rejected_w"] = rejected_w
    result_state["original_y"] = y.copy()
    result_state["original_w"] = w.copy()
    result_state["clean_w"] = clean_w
    result_state["st_dev_total"] = st_dev_total
    result_state["st_dev_below"] = st_dev_below
    result_state["st_dev_above"] = st_dev_above
    return result_state


def _set_final_vectors(y: np.ndarray, result_state: dict, flags: np.ndarray,
                       delta: float) -> dict:
    """Port of cpp/src/RCR.cpp:5132 — setFinalVectors(y), unweighted.

    Populates rejectedY, originalY, stDevAbove/Below/Total based on the
    cleanY distribution about its mean."""
    rejected_y = y[~flags].copy()
    clean_y = y[flags].copy()
    mean_hold = getMean(clean_y)

    clean_y_above: list[float] = []
    clean_y_below: list[float] = []
    w_above: list[float] = []
    w_below: list[float] = []
    for v in clean_y:
        adj = float(v) - mean_hold
        if isEqual(adj, 0.0):
            clean_y_above.append(adj)
            clean_y_below.append(adj)
            w_above.append(0.5)
            w_below.append(0.5)
        elif adj > 0.0:
            clean_y_above.append(adj)
            w_above.append(1.0)
        else:
            clean_y_below.append(adj)
            w_below.append(1.0)

    centered = clean_y - mean_hold
    st_dev_total = getStDev(delta, centered)
    cyb = np.array(clean_y_below, dtype=np.float64)
    cya = np.array(clean_y_above, dtype=np.float64)
    wb = np.array(w_below, dtype=np.float64)
    wa = np.array(w_above, dtype=np.float64)
    st_dev_below = getStDev_w(delta / 2.0, wb, cyb) if cyb.size else -1.0
    st_dev_above = getStDev_w(delta / 2.0, wa, cya) if cya.size else -1.0

    result_state["rejected_y"] = rejected_y
    result_state["original_y"] = y.copy()
    result_state["st_dev_total"] = st_dev_total
    result_state["st_dev_below"] = st_dev_below
    result_state["st_dev_above"] = st_dev_above
    return result_state


def performBulkRejection_LS(
    y: np.ndarray,
    rejection_tech_name: str,
    delta: float = 1.0,
    w: np.ndarray | None = None,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Port of cpp/src/RCR.cpp:5047 — performBulkRejection(y), unweighted.

    Four-pass:
      1. BULK with user's tech (uses handleBulkSigmaTechSelect's cascade)
      2. ITERATIVE with user's tech (sigmaTech and muTech as aligned)
      3. ITERATIVE median + 68th
      4. ITERATIVE mean + standard deviation
    Then setFinalVectors populates rejectedY/originalY/stDevTotal.
    """
    # Resolve tech → (pass1 mu_tech, pass1 sigma_tech, sigma_choice, CF fn)
    if rejection_tech_name == "LS_MODE_68":
        pass1_mu, pass1_sigma = MuTech.MODE, SigmaTech.SIXTY_EIGHTH_PERCENTILE
        sigma_choice = SigmaChoice.LOWER
        n_correct_unw, n_correct_wgt = getLower68CF, getLower68CF_w
    elif rejection_tech_name == "LS_MODE_DL":
        pass1_mu, pass1_sigma = MuTech.MODE, SigmaTech.DOUBLE_LINE
        sigma_choice = SigmaChoice.LOWER
        n_correct_unw, n_correct_wgt = getLowerDLCF, getLowerDLCF_w
    elif rejection_tech_name == "SS_MEDIAN_DL":
        pass1_mu, pass1_sigma = MuTech.MEDIAN, SigmaTech.DOUBLE_LINE
        sigma_choice = SigmaChoice.SINGLE
        n_correct_unw, n_correct_wgt = getSingleDLCF, getSingleDLCF_w
    elif rejection_tech_name == "ES_MODE_DL":
        pass1_mu, pass1_sigma = MuTech.MODE, SigmaTech.DOUBLE_LINE
        sigma_choice = SigmaChoice.EACH
        n_correct_unw, n_correct_wgt = getEachDLCF, getEachDLCF_w
    else:
        raise NotImplementedError(f"unknown rejection_tech_name {rejection_tech_name!r}")

    flags = np.ones(y.size, dtype=bool)

    # When PARAMETRIC, the C++ sets delta = parameterSpace.size() = M
    # (number of model params) so getStDev's denominator changes.
    if parametric_model is not None:
        delta = float(parametric_model.M)

    mods = (non_parametric_model, parametric_model)

    if w is None:
        # Pass 1: bulk
        if sigma_choice is SigmaChoice.LOWER:
            bulkLowerSigmaRCR(y, flags, pass1_mu, n_correct_unw, *mods)
            iter_loop = iterativeLowerSigmaRCR
        elif sigma_choice is SigmaChoice.SINGLE:
            bulkSingleSigmaRCR(y, flags, pass1_mu, n_correct_unw, *mods)
            iter_loop = iterativeSingleSigmaRCR
        else:  # EACH
            bulkEachSigmaRCR(y, flags, pass1_mu, n_correct_unw, *mods)
            iter_loop = iterativeEachSigmaRCR
        iter_loop(y, flags, pass1_mu, pass1_sigma, delta, n_correct_unw, *mods)
        iter_loop(y, flags, MuTech.MEDIAN, SigmaTech.SIXTY_EIGHTH_PERCENTILE, delta, n_correct_unw, *mods)
        state = iter_loop(y, flags, MuTech.MEAN, SigmaTech.STANDARD_DEVIATION, delta, n_correct_unw, *mods)

        kept = np.where(flags)[0]
        out = {"flags": flags, "indices": kept, "clean_y": y[kept].copy(), **state}
        _set_final_vectors(y, out, flags, delta)
        return out

    # Weighted bulk
    if sigma_choice is SigmaChoice.LOWER:
        bulkLowerSigmaRCR_w(w, y, flags, pass1_mu, n_correct_wgt, *mods)
        iter_loop_w = iterativeLowerSigmaRCR_w
    elif sigma_choice is SigmaChoice.SINGLE:
        bulkSingleSigmaRCR_w(w, y, flags, pass1_mu, n_correct_wgt, *mods)
        iter_loop_w = iterativeSingleSigmaRCR_w
    else:
        bulkEachSigmaRCR_w(w, y, flags, pass1_mu, n_correct_wgt, *mods)
        iter_loop_w = iterativeEachSigmaRCR_w
    iter_loop_w(w, y, flags, pass1_mu, pass1_sigma, delta, n_correct_wgt, *mods)
    iter_loop_w(w, y, flags, MuTech.MEDIAN, SigmaTech.SIXTY_EIGHTH_PERCENTILE, delta, n_correct_wgt, *mods)
    state = iter_loop_w(w, y, flags, MuTech.MEAN, SigmaTech.STANDARD_DEVIATION, delta, n_correct_wgt, *mods)

    kept = np.where(flags)[0]
    out = {"flags": flags, "indices": kept, "clean_y": y[kept].copy(), **state}
    _set_final_vectors_w(w, y, out, flags, delta)
    return out


def performRejection_LS(
    y: np.ndarray,
    rejection_tech_name: str,
    delta: float = 1.0,
    w: np.ndarray | None = None,
    non_parametric_model=None,
    parametric_model=None,
) -> dict:
    """Port of cpp/src/RCR.cpp:5017 — performRejection(y), restricted to the
    LS_MODE_68 / LS_MODE_DL rejection techs (sigmaChoice=LOWER, muTech=MODE).

    The C++ does three iterative passes, each mutating flags further:
        Pass 1: (mu = MODE,   sigma = user's sigmaTech)  -- aligned by rejection_tech
        Pass 2: (mu = MEDIAN, sigma = 68th percentile)
        Pass 3: (mu = MEAN,   sigma = standard deviation)
    Then `alignTechniques()` resets state — irrelevant once results are
    captured.
    """
    # Resolve (pass1_mu, pass1_sigma, n_correct, sigma_choice) from rejection tech
    # via the alignTechniques table at cpp/src/RCR.cpp:1539.
    if rejection_tech_name == "LS_MODE_68":
        pass1_mu = MuTech.MODE
        pass1_sigma = SigmaTech.SIXTY_EIGHTH_PERCENTILE
        sigma_choice = SigmaChoice.LOWER
        n_correct_unw, n_correct_wgt = getLower68CF, getLower68CF_w
    elif rejection_tech_name == "LS_MODE_DL":
        pass1_mu = MuTech.MODE
        pass1_sigma = SigmaTech.DOUBLE_LINE
        sigma_choice = SigmaChoice.LOWER
        n_correct_unw, n_correct_wgt = getLowerDLCF, getLowerDLCF_w
    elif rejection_tech_name == "SS_MEDIAN_DL":
        pass1_mu = MuTech.MEDIAN
        pass1_sigma = SigmaTech.DOUBLE_LINE
        sigma_choice = SigmaChoice.SINGLE
        n_correct_unw, n_correct_wgt = getSingleDLCF, getSingleDLCF_w
    elif rejection_tech_name == "ES_MODE_DL":
        pass1_mu = MuTech.MODE
        pass1_sigma = SigmaTech.DOUBLE_LINE
        sigma_choice = SigmaChoice.EACH
        n_correct_unw, n_correct_wgt = getEachDLCF, getEachDLCF_w
    else:
        raise NotImplementedError(
            f"unsupported rejection technique: {rejection_tech_name}"
        )

    flags = np.ones(y.size, dtype=bool)

    if sigma_choice is SigmaChoice.LOWER:
        loop_unw = iterativeLowerSigmaRCR
        loop_wgt = iterativeLowerSigmaRCR_w
    elif sigma_choice is SigmaChoice.SINGLE:
        loop_unw = iterativeSingleSigmaRCR
        loop_wgt = iterativeSingleSigmaRCR_w
    else:  # EACH
        loop_unw = iterativeEachSigmaRCR
        loop_wgt = iterativeEachSigmaRCR_w

    # All loop families now accept (non_parametric_model, parametric_model)
    # as the trailing two args.
    extra_unw = (non_parametric_model, parametric_model)
    extra_wgt = (non_parametric_model, parametric_model)

    if w is None:
        state = loop_unw(y, flags, pass1_mu, pass1_sigma, delta, n_correct_unw, *extra_unw)
        state = loop_unw(y, flags, MuTech.MEDIAN, SigmaTech.SIXTY_EIGHTH_PERCENTILE, delta, n_correct_unw, *extra_unw)
        state = loop_unw(y, flags, MuTech.MEAN, SigmaTech.STANDARD_DEVIATION, delta, n_correct_unw, *extra_unw)
    else:
        state = loop_wgt(w, y, flags, pass1_mu, pass1_sigma, delta, n_correct_wgt, *extra_wgt)
        state = loop_wgt(w, y, flags, MuTech.MEDIAN, SigmaTech.SIXTY_EIGHTH_PERCENTILE, delta, n_correct_wgt, *extra_wgt)
        state = loop_wgt(w, y, flags, MuTech.MEAN, SigmaTech.STANDARD_DEVIATION, delta, n_correct_wgt, *extra_wgt)

    indices_kept = np.where(flags)[0]
    indices_rejected = np.where(~flags)[0]

    out = {
        "flags": flags,
        "indices": indices_kept,
        "clean_y": y[indices_kept].copy(),
        "rejected_y": y[indices_rejected].copy(),
        "original_y": y.copy(),
        **state,
    }
    if w is not None:
        out["clean_w"] = w[indices_kept].copy()
    return out

# ============================================================================
# Module: api.py
# ============================================================================





class RejectionTech(Enum):
    SS_MEDIAN_DL = "SS_MEDIAN_DL"
    LS_MODE_68 = "LS_MODE_68"
    LS_MODE_DL = "LS_MODE_DL"
    ES_MODE_DL = "ES_MODE_DL"


class MuType(Enum):
    """How `mu` is computed each iteration. VALUE is the default (use the
    handled muTech directly on `y`); PARAMETRIC and NONPARAMETRIC delegate
    to a model object that the user attaches via setter methods on RCR.
    Ports of cpp/src/RCR.h::MuTypes."""
    VALUE = "VALUE"
    PARAMETRIC = "PARAMETRIC"      # FunctionalForm — not yet wired in rcrpy
    NONPARAMETRIC = "NONPARAMETRIC"


@dataclass
class RCRResults:
    mu: float = float("nan")
    sigma: float = float("nan")
    sigma_below: float = float("nan")
    sigma_above: float = float("nan")
    st_dev: float = float("nan")
    st_dev_below: float = float("nan")
    st_dev_above: float = float("nan")
    st_dev_total: float = float("nan")
    flags: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=bool))
    indices: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.int64))
    clean_y: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    rejected_y: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    clean_w: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    rejected_w: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    original_y: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))
    original_w: np.ndarray = field(default_factory=lambda: np.empty(0, dtype=np.float64))


class RCR:
    """Single-value Robust Chauvenet Rejection.

    Phase 1: API skeleton. The real iterative + bulk loops are ported in
    rejection and dispatched here.
    """

    def __init__(self, rejection_tech: RejectionTech = RejectionTech.SS_MEDIAN_DL):
        self.rejection_tech = rejection_tech
        self.result = RCRResults()
        self.mu_type: MuType = MuType.VALUE
        self.non_parametric_model = None  # set via set_non_parametric_model
        self.parametric_model = None      # set via set_parametric_model

    def set_rejection_tech(self, tech: RejectionTech) -> None:
        self.rejection_tech = tech

    def set_mu_type(self, mu_type: MuType) -> None:
        self.mu_type = mu_type

    def set_non_parametric_model(self, model) -> None:
        """Attach a `NonParametric` subclass instance. The user must
        also call `set_mu_type(MuType.NONPARAMETRIC)`. Port of
        cpp/src/RCR.h::setNonParametricModel."""
        self.non_parametric_model = model
        self.mu_type = MuType.NONPARAMETRIC

    def set_parametric_model(self, model) -> None:
        """Attach a `FunctionalForm` instance for model-fitting RCR.
        Sets mu_type to PARAMETRIC automatically. Port of
        cpp/src/RCR.h::setParametricModel."""
        self.parametric_model = model
        self.mu_type = MuType.PARAMETRIC

    def perform_rejection(
        self,
        y: Sequence[float],
        w: Sequence[float] | None = None,
    ) -> None:
        y_arr = np.asarray(y, dtype=np.float64)
        w_arr = None if w is None else np.asarray(w, dtype=np.float64)
        out = performRejection_LS(
            y_arr, self.rejection_tech.value, w=w_arr,
            non_parametric_model=(self.non_parametric_model
                                   if self.mu_type is MuType.NONPARAMETRIC else None),
            parametric_model=(self.parametric_model
                              if self.mu_type is MuType.PARAMETRIC else None),
        )
        r = self.result
        r.mu = out["mu"]
        r.flags = out["flags"]
        r.indices = out["indices"]
        r.clean_y = out["clean_y"]
        # Each-sigma populates sigma_below/above; the other techs populate
        # sigma/st_dev. setFinalVectors-only fields (rejectedY, originalY,
        # clean_w/rejected_w/original_w) remain at default-empty per
        # performRejection's contract.
        if "sigma" in out:
            r.sigma = out["sigma"]
            r.st_dev = out["st_dev"]
        if "sigma_below" in out:
            r.sigma_below = out["sigma_below"]
            r.sigma_above = out["sigma_above"]
        if "st_dev_above" in out:
            r.st_dev_above = out["st_dev_above"]
        if "st_dev_below" in out:
            r.st_dev_below = out["st_dev_below"]
        if "clean_w" in out:
            r.clean_w = out["clean_w"]
        # NOTE: rejected_y / original_y / clean_w / rejected_w / original_w are
        # NOT set here — the C++ performRejection (non-bulk) leaves them at
        # their default-empty state; only performBulkRejection populates them
        # via setFinalVectors. Match that contract for parity.

    def perform_bulk_rejection(
        self,
        y: Sequence[float],
        w: Sequence[float] | None = None,
    ) -> None:
        y_arr = np.asarray(y, dtype=np.float64)
        w_arr = None if w is None else np.asarray(w, dtype=np.float64)
        out = performBulkRejection_LS(
            y_arr, self.rejection_tech.value, w=w_arr,
            non_parametric_model=(self.non_parametric_model
                                   if self.mu_type is MuType.NONPARAMETRIC else None),
            parametric_model=(self.parametric_model
                              if self.mu_type is MuType.PARAMETRIC else None),
        )
        r = self.result
        r.mu = out["mu"]
        r.flags = out["flags"]
        r.indices = out["indices"]
        r.clean_y = out["clean_y"]
        r.rejected_y = out["rejected_y"]
        r.original_y = out["original_y"]
        r.st_dev_total = out["st_dev_total"]
        r.st_dev_above = out["st_dev_above"]
        r.st_dev_below = out["st_dev_below"]
        # Single/Lower set sigma + st_dev; Each sets sigma_below/sigma_above.
        if "sigma" in out:
            r.sigma = out["sigma"]
            if "st_dev" in out:
                r.st_dev = out["st_dev"]
        if "sigma_below" in out:
            r.sigma_below = out["sigma_below"]
            r.sigma_above = out["sigma_above"]
        if "clean_w" in out:
            r.clean_w = out["clean_w"]
        if "rejected_w" in out:
            r.rejected_w = out["rejected_w"]
        if "original_w" in out:
            r.original_w = out["original_w"]

__all__ = [
    "RCR",
    "RCRResults",
    "RejectionTech",
    "MuType",
    "FunctionalForm",
    "FunctionalFormResults",
    "Priors",
    "PriorType",
    "NonParametric",
]
__version__ = "0.1.0"
